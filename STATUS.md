# Status

## Current phase
Phase 14 — Packaging is next (Phases 19, 20, 21, and 22 are all done on the code side; see below). Carried into it: Phase 21's six unrun live-verification items and **all of Phase 22's live-verification items** — both sets are gated on model credits/TCC, not on code — plus `bugs.spec.md` #6, #8, #9, all still open.

**Out-of-phase addition this session: `windower daemon kill`.** User-facing gap — `daemon stop` only works over the graceful RPC socket, so a hung/unreachable daemon (or its native sidecar children) had no cleanup path but manual `ps`/`kill`. Added `windower daemon kill [--json]` (`packages/cli/src/commands/daemon.ts`) — force-kills by OS pid, bypassing the socket entirely: reads `~/.windower/daemon.json` for the daemon pid and the new `~/.windower/sidecar-pids.json` (`packages/core/src/daemon/sidecar-pids.ts`) for native sidecar child pids the daemon spawned, SIGTERM-then-SIGKILL each, clears both state files plus a matching stale `capture.lock`. `sidecar-pids.json` is newly written/cleared by the daemon itself (`apps/daemon/src/main.ts`'s `trackedSpawnSidecar` wrapper around `spawnSidecar`, `apps/daemon/src/server.ts`'s `stop()`) since a sidecar pid otherwise only lived in the daemon's own memory and a fresh CLI process has no way to discover it. Scoped deliberately to daemon + its own sidecars only — never `mcp-server` processes. `contracts/cli.md`'s Daemon policy table and `daemon status|stop|restart|kill` section updated; `policy.ts`/`policy.test.ts` (`CommandId`/`POLICY_TABLE`) updated with a `"daemon kill": "local"` entry (documentation/coverage only — the real command bypasses `withBackend` entirely, see the code comment). New/updated tests: `packages/core/src/daemon/sidecar-pids.test.ts`, `killDaemonAndSidecars` cases in `packages/core/src/daemon/connect.test.ts`, `packages/cli/src/commands/daemon.test.ts`. Full `pnpm turbo build`/`pnpm turbo test` green (19/19 tasks). windower-site's CLI reference/troubleshooting docs updated to match — see that repo's own status/changelog if present.

Phase 22 — Operator: AX-First Observation and the Planner/Executor Split (v1.5): **code complete, all suites green — live verification NOT run, pending manual/TCC-gated pass.** Implementation matches `tasks/phase-22-operator-ax-first.md` end to end: `native/macos/Sources/WindowerControlCore/ElementQuery.swift` (new, control-surface only — imports `ApplicationServices` alone, `check-no-screencapturekit.sh` still passing) walks `kAXChildrenAttribute` breadth-first, ref/generation staleness, points→pixels conversion reusing `InputCoordinateSpace`; `enumerateElements` is wired through `packages/core` (schemas, `SIDECAR_METHODS`, `SidecarClient`, `fake-sidecar.ts` with `ui.elements` removable from `DEFAULT_CAPABILITIES`), `packages/engine` (`control-engine.ts` passthrough, `operator-loop-host.ts` serving the loop-protocol method, daemon-authoritative `maxReplans`), and `packages/operator` (five new element tools, `--observe auto|ax|vision`, planner/executor tiers in `providers.ts`/`run.ts`, Anthropic-only system-prompt `cache_control`, `prompt.ts` rewritten for the element-list-as-default percept). CLI/MCP gained `--planner-model`/`--executor-model`/`--observe`/`--max-replans` (`packages/cli/src/commands/operate-params.ts`); `plugins/claude-code/SKILL.md` updated. Docs (this session): `contracts/operator.md` fully rewritten (tool table 15→20, new §Observation policy and §Model tiers, amended caching sentence corrected to match the as-shipped Anthropic-only/system-prompt-only scope in `providers.ts` rather than the phase file's original system-prompt-**and**-tool-schema plan, `maxReplans` in the guardrail table, "four" → "five" sidecar-facing methods, `ObservationRef` documented as a breaking transcript-schema change with older transcripts explicitly read-only/not migrated); `CLAUDE.md`'s Operator bullet already stated the AX-is-a-sensor-never-an-actuator invariant accurately and needed no change; `specs/001-windower-mvp/contracts/operator-loop-protocol.md`, `contracts/sidecar-protocol.md`, `data-model.md`, `research.md` §2, and `spec.md` §5 were already updated by an earlier session and were cross-checked for consistency, not rewritten.

**Exit criteria status** (from the phase file's "Exit criteria" section) — code/test-verified this session unless flagged otherwise:
- `windower-control-macos` links no ScreenCaptureKit after `ElementQuery.swift` — **verified**, `check-no-screencapturekit.sh` passing.
- Single-`--model` parity (no tier flags behaves exactly as pre-Phase-22) — **verified by test**, `packages/operator/src/phase22.test.ts` §"single-model parity".
- Prompt-caching-disabled parity (identical transcript with caching on vs. off/unsupported) — **verified by test**, same file.
- `ui.elements`-absent degrades to vision-only, no error, no platform branch — **verified by test** (`phase22.test.ts` §"ui.elements-absent") and by `rg 'platform === "macos"'` over `packages/`/`apps/` returning nothing.
- `AX_ELEMENT_STALE` is non-terminal, run recovers — **verified by test**, `phase22.test.ts`.
- `maxReplans` terminates a non-converging run with `OPERATOR_MAX_REPLANS_EXCEEDED` rather than exhausting `maxSteps` — **verified by test**, `phase22.test.ts`.
- Recording-independence tests and the loop-child dependency-graph test pass unchanged, now proxying five methods — **verified**, existing suites green with no new failures.
- Element rects land correct clicks on a scale-2 display — **partially verified**: the Swift points→pixels unit test (`ElementQueryTests.swift`) is green; **the paired live click on a real Retina display is not done** (TCC/hardware-gated).
- A run against a native macOS app with `--observe ax` completes with zero frames captured, counted from real transcript `observations[].kind` — **NOT verified live**; the policy logic that makes this true (`run.ts`'s `wantsFrame` gate, unconditionally `false` under `"ax"`) is unit-tested against a fake sidecar, but no real transcript has been produced and inspected.
- The waroom.co task completes in materially fewer steps and model calls than the Phase 21 baseline (29/30/29 at `--max-steps 30`) — **NOT measured**; needs a real run, see "Baseline comparison" below.
- Full Swift + TS suites green with no regressions — **as reported by the implementing agents this session; not independently re-run end-to-end by this docs pass** (this session's scope was documentation only, per the task brief).

**Baseline comparison (placeholder — real numbers require a live run).** Phase 21 baseline: 29 / 30 / 29 steps at `--max-steps 30`, one full-strength model call per step (one `generateText` call = one step, pre-Phase-22), one 1280px PNG captured every step, no token/dollar figures (never collected). Phase 22 post-implementation numbers — steps, planner-vs-executor model call counts, frames captured (should trend toward the `observations[].kind === "elements"` share, ideally near-total under `--observe ax` against a native app), and wall-clock — **are not yet recorded**, because they require the live waroom.co run in "Live verification" below. Do not fill this row in from code reading or estimation; it must come from an actual transcript.

**Live verification (Phase 22) — entirely pending, out of scope for this and any prior docs/implementation session.** Per `e2e/README.md`'s manual/TCC-gated convention, the five items in `tasks/phase-22-operator-ax-first.md`'s "Live verification" section require real TCC grants, a real funded model account, and a real target app or `waroom.co` — none of which a coding agent can obtain non-interactively. **The user must run these manually:** the motivating waroom.co run (strong planner + cheap executor) compared against the 29/30/29 baseline; recording-independence parity (bare vs. `start_recording`-wrapped, confirming zero capture calls on AX-observed steps in both cases); vision-fallback engagement on a web/canvas-heavy target; stale-ref recovery (move/resize the target window mid-run); and single-model-parity live. None of these are this or any subagent's responsibility to attempt.

Phase 19 — Operator: **done**, exercised end-to-end against a real LLM with real TCC grants this session — see "Live verification results" below.

Phase 20 — Daemon-Optional: **done**, including live verification. All 6 items in the phase's "Live verification" checklist (`tasks/phase-20-daemon-optional.md`) passed on real hardware with real TCC grants and a real model — see "Live verification results" below. This also clears Phase 19's outstanding e2e debt.

Phase 21 — Capture/Control Process Split and ScreenCaptureKit Exclusivity: **done** (marked complete 2026-08-10 in `tasks/INDEX.md` and the phase file). Implementation complete and green; **live verification partially run — the core repro passed 3/3 at 99.9%, six model-gated items were not run because the Anthropic account ran out of credits mid-session** and the product owner elected to close the phase rather than switch providers. The phase is closed; the six outstanding items and `bugs.spec.md` #6 are not — see below. Every code, test, and doc task in `tasks/phase-21-capture-control-broker.md` is done. Of the phase's final task — the manual, TCC-gated live-verification checklist — **six of twelve items passed on real hardware, six could not be run**; see "Live verification results (Phase 21, run this session)" below for the exact split. **`bugs.spec.md` #6 remains open and its closing entry is deliberately still unwritten.** The single most important result is that the replayd-conflict truncation class did not recur across three real 3.5–5-minute caller-driven runs (99.9% duration-preserved on every run, zero `didStopWithError` in `log stream` traces of all three) — but the crash-injection and stress items that would prove isolation under failure are outstanding, so the phase is **not** done and #6 must not be closed yet.

The historical note below describes the state at the *start* of that work and is kept for context.

Phase 21 (historical, at session start) — **spec/contracts complete and approved; implementation partially done and partially requiring revert.** See `tasks/phase-21-handoff-prompt.md` for the exact resume prompt. The architecture went through three product-owner corrections this session and the final approved shape is: **the user's coding agent is the orchestrator; Windower exposes three independent capabilities (Capture, Operator, Control) and none owns or references another.** Concretely settled: the Operator is completely recording-unaware (no `sessionId`, no attach mode, no standalone recording mode — a breaking change to Phase 19's `run_operator`); there is **no Capture Broker** component (no discovery, routing, IPC socket, or cross-process RPC — only a `~/.windower/capture.lock` file mutex enforcing the macOS ScreenCaptureKit single-writer invariant created by Phase 20's daemon-optional execution); orphan prevention is **process ownership** (capture child exits on stdin EOF), not pid tracking. Done and green: the native `windower-capture-macos`/`windower-control-macos` split (231 Swift tests, `otool -L` regression check enforcing that the control binary cannot link ScreenCaptureKit), the two-surface `sidecar-path.ts` resolver and packaging, and `packages/core`'s plan/batching schemas. Requiring revert: `packages/engine/src/broker-lock.ts`'s routing/socket/promotion layer, operator `sessionId`/attach mode across core+engine+CLI, and `OutputManifest.operatorRunPath`. Verified this session and **not yet fixed**: `windower-capture-macos` exits on stdin EOF (`main.swift:335-345`) but performs no capture cleanup on that path — an EOF mid-recording exits without stopping the `SCStream` or finalizing the `AVAssetWriter`. That is the phase's first implementation task.

**Prior focus — `bugs.spec.md` #6 (capture silently truncates on longer recordings): root mechanism now identified with direct OS-level log evidence and substantially fixed — do not re-open as "not fixed" without new evidence of the specific mechanism below recurring.** OS-level tracing (`log stream` bracketing a live repro, finally attempted after being recommended by 3+ prior sessions) found the actual mechanism: `packages/engine/src/passthrough.ts`'s `PassthroughService.listTargets`/`resizeWindow` spawn a brand-new, separate `windower-sidecar-macos` process per call and terminate it immediately after (by design, for callers with no active session) — but the operator loop's `list_targets`/`resize_window` tools (in both `packages/engine/src/operator-run-engine.ts` and a second, independent implementation in `packages/cli/src/commands/operate-blocking.ts`, the actual code path `windower operate` runs by default) routed through this passthrough **even while a recording's own sidecar was actively streaming**. The transient second process's brief `SCShareableContent` call + exit reliably causes `replayd` to invalidate the *first* process's live stream — confirmed via a timestamped log excerpt: `windower-sidecar-macos[52257:...]` (a second PID, not the recording's `51729`) calls `SCShareableContent.getShareableContentWithCompletionHandler`, exits 53ms later, and 4ms after that `replayd` logs `RPClientProxy stream:didStopWithError:` for the *other* process's stream. This is a `replayd`/WindowServer-level resource-scope conflict between two processes, not a per-call ScreenCaptureKit API incompatibility — which is exactly why 4+ prior sessions' raw single-process repros (issuing `enumerateTargets`/`captureFrame` within the SAME process already holding the stream) never reproduced it. **Fix:** both `createDeps` implementations now call `ctx.client.enumerateTargets`/`ctx.client.resizeWindow` directly (the same already-running sidecar as the live capture) instead of `passthrough.listTargets`/`resizeWindow`, so no second ScreenCaptureKit process is ever spawned while a stream is live. No `native/macos`/protocol change — TS-only fix (`swift build`/`swift test` untouched, still 191/191). New tests in `operator-run-engine.test.ts`/`operate-blocking.test.ts` assert no extra sidecar spawn occurs for in-loop `list_targets`/`resize_window` calls. Quantitatively confirmed via a real `windower operate` run (Finder→Applications→Safari→waroom.co, `anthropic:claude-sonnet-5`, `--max-steps 30`, OS log tracing throughout): **81.2% of a real 203.7s session preserved** (165,492ms video / 203,734ms wall clock), vs. two prior confirmed-bad baselines of 59.5% and 2.7% — a large, real improvement, and `grep`-verified exactly 2 sidecar processes existed for the entire run despite 3 mid-run `list_targets` calls. **Honest remaining gap:** 81.2% is not 100% — the same run showed a milder, self-recovering intermittent-stall pattern (37 PTS-gap stalls, one early liveness trip that cleared) consistent with ordinary `SCStream`/encoder backpressure under heavy real interaction, structurally different from (and much less severe than) the permanent, error-free, second-process-triggered death this session's fix targets. See `specs/001-windower-mvp/bugs.spec.md` #6's latest dated entry for the full log excerpt, before/after table, and what's still open.

**Follow-up session on the residual ~19%/37-stall issue: blocked by an invalid `ANTHROPIC_API_KEY`, no live-model repro possible.** Extensive raw-sidecar stress testing (real Safari launch + navigation, rapid double-click bursts, full 14-core CPU saturation via background `yes` processes, all against the single already-fixed sidecar process) found **zero** reproduction across three ~180s runs — video duration matched wall clock exactly every time, zero drops/stalls/liveness failures. This is a genuine negative result consistent with this bug's entire history (raw single-process repros have never reproduced any confirmed occurrence, catastrophic or mild). No behavioral fix was shipped — Apple's live-capture guidance already matches the current drop-on-backpressure code, and changing it without evidence would be speculative. What *was* shipped: `native/macos/Sources/WindowerSidecarCore/CaptureService.swift`'s existing liveness-watchdog timer now also emits a real-time `warn` `log` notification on every NEW PTS-gap stall (previously only visible as a rolled-up count at `stopCapture`), so the next live repro with a working API key can correlate stall timestamps against `.events.json`/operator steps directly instead of re-deriving this instrumentation. `swift build`/`swift test` still 191/191, no regressions. Bug #6's main (catastrophic) mechanism remains fixed and should not be re-opened; the residual milder issue is unchanged in status — still open, still believed minor, root cause still not found. See `specs/001-windower-mvp/bugs.spec.md` #6's latest dated entry for full methodology and numbers.

**Follow-up session — API key fixed, one live repro obtained and correlated, no code fix applied.** With a working key confirmed directly against `api.anthropic.com` (`model: "claude-sonnet-5"`), ran a real `windower operate` (Finder→Applications→Safari→waroom.co, `anthropic:claude-sonnet-5`, `--max-steps 30`, OS log tracing throughout, no daemon). Reached `state: "succeeded"` in 23 steps with **~101% of the session preserved** (174,151.67ms video, `ffprobe`-verified exact match, vs. 172,372ms wall clock) — the catastrophic mechanism remains fixed. The run did show the residual milder pattern: **13 PTS-gap stalls (longest gap 5,125ms), zero dropped frames**, and correlating stall timestamps against the operator's own step log (table in `bugs.spec.md`) shows they cluster entirely in the Safari/waroom.co portion (steps 8–23, t≈100s–172s) with **zero stalls** during the earlier Finder/Dock portion (steps 0–7, t≈4.5s–96.5s) — and do **not** correlate consistently with any specific RPC type (`list_targets` calls have zero adjacent stalls; `captureFrame`/screenshot calls are sometimes followed by a stall and sometimes not). This points to ordinary `SCStream`/compositor backpressure under real, dynamic, network-loaded page content, not an RPC-triggered or writer-backpressure mechanism — and since `droppedFrameCount` was 0 throughout, none of this task's three candidate fixes (cache-latest-frame for `captureFrame`, queue-instead-of-drop for `VideoAssetWriter`, or a hybrid) are supported by the evidence, so **no Swift code was changed this session** (`swift build`/`swift test` still 191/191, unmodified). A second confirmation run was started (already showing 8 stalls by ~78s) but was user-cancelled before completion — only one live run's data is used, and this entry is explicit that it does not claim two confirmed runs. Residual issue remains open; next step is OS-level tracing specifically bracketing a clustered-stall window (same technique that found the catastrophic mechanism's root cause), not yet applied to this milder pattern. See `specs/001-windower-mvp/bugs.spec.md` #6's latest dated entry for the full step/stall correlation table.

## Done
- Phase 21 — Capture/Control Process Split and ScreenCaptureKit Exclusivity (v1.4): **done — code complete, all suites green, live verification 6/12 PASS 0 FAIL 6 not run** (see the Phase 21 live-verification section below for the exact split and what it does and does not license concluding).
  - **Governing architecture (settled across three product-owner corrections; do not relitigate):** the user's coding agent is the orchestrator; Windower exposes three **independent peer capabilities** — Capture, Operator, Control — and **none owns or references another**. Windower ships no code, type, or RPC performing `start_recording → run_operator → wait → stop_recording`; that recipe lives in `plugins/claude-code/SKILL.md` and nowhere else.
  - **Reverts of superseded in-phase work** (a prior session built against designs since rejected): the "capture broker" with its discovery/routing/IPC-socket/holder-promotion layer is deleted — `broker-lock.ts` → `screen-capture-lock.ts`, `BrokerLock` → `CaptureLock`, `CaptureBrokerBusyError` → `ScreenCaptureBusyError` (`CAPTURE_BROKER_BUSY` → `SCREEN_CAPTURE_BUSY`), `CaptureBroker` → `CaptureAccess`, `~/.windower/broker.lock` → `capture.lock`. Operator `sessionId`/attach mode and operator-owned standalone recording are deleted across core/engine/CLI/MCP (an accepted **breaking change** to Phase 19's `run_operator`). `OutputManifest.operatorRunPath` and `RecordingEngine`'s whole `operatorRunPaths`/`setOperatorRunPath` mechanism are gone, along with `getSidecarClient`/`getPlannedOutputPath` (same reverse dependency, no other callers). `LoopAbortReason` lost `"session-ended"`. **A repo-wide `grep -i broker` is clean** — a literal exit criterion.
  - **`native/macos`**: EOF-triggered capture cleanup — on stdin EOF the capture sidecar drains in-flight RPCs, then `CaptureSessionManager.stopAllSessions()` (a loop over the existing `stopCapture`, so teardown order is reused not re-derived) behind a **10s budget**, then `exit(0)`. So EOF mid-recording finalizes the `AVAssetWriter` instead of leaving a moov-less file. Control sidecar's EOF is a plain exit (no capture state), confirmed and tested. **No pid tracking, reaper, or supervisor was introduced** — orphan prevention is process ownership only, and the lock payload is still exactly `{ pid, acquiredAt, windowerHome }`. New XCTests for both binaries plus an e2e test that `kill -9`s the parent and polls `ps` for a surviving capture child (matched by pid+command, since an orphan reparents to pid 1).
  - **ScreenCaptureKit exclusivity**: `packages/engine/src/screen-capture-lock.ts` implements the contract's four-row acquire-or-wait table (reuse → acquire-and-spawn → bounded wait → `SCREEN_CAPTURE_BUSY`; foreign `WINDOWER_HOME` fails immediately), 5 ms→100 ms backoff on a 2000 ms budget, pid-liveness stale-steal, refcounted in-process sharing. **No routing, discovery, IPC socket, or holder promotion** — a caller is never routed to the holder. The daemon builds **one** `CaptureLock` and **one** `ControlEngine` in `main.ts` and injects them into `RecordingEngine`/`PassthroughService`/`OperatorRunEngine`, so "exactly one capture process inside the daemon" holds by construction rather than by which sidecar happens to be reused.
  - **Three real surface-resolution bugs found and fixed.** `sidecar-process.ts` resolves `options.surface ?? "capture"`, so *omitting* `surface` silently yields the capture binary: `passthrough.ts`'s `resizeWindow` fallback, `operator-run-engine.ts`'s default `ControlEngine`, and `operate-blocking.ts`'s `ControlEngine` were each spawning the **capture** binary for control-surface calls — i.e. a second ScreenCaptureKit process, the exact hazard this phase exists to remove. Separately, **`windower doctor` spawned a capture sidecar (twice) with no capture lock** from a daemon-free process, so running `doctor` during a live recording was itself a second SCK consumer — now one locked probe, and a busy lock yields a diagnostic naming the holder rather than a crash. `doctor` also now merges capture+control permission reports (absent kinds render **unknown, never denied**).
  - **Operator loop as a child process**: `contracts/operator-loop-protocol.md` implemented — `OperatorLoopHost` (`packages/engine/src/operator-loop-host.ts`) spawns one child per run, handshakes via `ready` with **nothing on argv**, and serves all ten child→daemon methods over bidirectional newline-delimited JSON-RPC with independent id spaces. The in-process `loadRunOperator` seam is gone. **Guardrails are daemon-side and authoritative** (`beginStep` is the only counter increment; screen-facing methods require an open step; `actionsInStep` counts only actions; the bounds clamp and secret substitution run per action request on arrival) — the child keeps its own copies as deliberate duplication, not redundancy to refactor away. The child receives `secretNames` only and **never a resolved secret value**. `OPERATOR_LOOP_CRASHED` preserves the partial transcript. **The crash path has no branch that mentions recording** — with-recording and without-recording are literally one code path.
  - **Operator reasoning**: explicit `plan` stage, safe action batching (`maxBatchActions`, default 8, non-terminal on overflow, `BATCH_ABORTED` rows so "ran / failed / never ran" is stated not inferred), and a new **`checkpoint` tool** completing `plan → execute → observe → checkpoint → continue/replan`. Product-owner ruling, recorded so it isn't rediscovered: **checkpoint outcomes are never inferred by the runtime** — "the turn replanned" is not a proxy for `failed-plan-invalid` and its absence is not a proxy for `held`, because only the turn that observed the screen knows whether an expectation was met. Pinned by a matched test pair.
  - **Contract fixes made before implementation** (per `CLAUDE.md`): `data-model.md`'s `OperatorRun.target` was the unresolved selector union while `contracts/operator.md` said resolved — a persisted unresolved selector would defeat the bounds clamp, so the record now holds the resolution (input still takes the selector). `LoopReadyResultSchema` was missing `target` and `maxBatchActions` that §Handshake already showed. `OperatorStep.checkpoint` existed in two contracts but in neither `data-model.md` nor the schema. `OperatorRun.summary` did not exist, so a model's `done({summary})` rode an in-memory event with no subscriber and **no caller could ever read it** — which falsified §"How they surface"'s claim that polling `get_operator_run` sees every event's content.
  - **Test-isolation bug worth remembering:** `apps/daemon/src/server.test.ts` set a per-test `windowerHome` **variable** but never `process.env.WINDOWER_HOME`, so the suite read and wrote the developer's **real `~/.windower`**. Because `CaptureLock`'s hold registry is module-global and keyed by lock path, a test that deliberately leaves a recording live leaked its fake sidecar into later tests, which silently reused it instead of their own restricted fake — presenting as 4 "pre-existing" failures. A stale real `capture.lock` (dead pid) added 2 s bounded waits on top. Daemon suite went 4-failed/41 s → **22/22 in 0.9 s**. Every suite touching the lock now sets `WINDOWER_HOME` to a temp dir and calls `resetCaptureHoldsForTesting()`; `doctor`'s suite also sets `WINDOWER_CONTROL_BINARY_PATH`, without which it probes the developer's real Accessibility grant.
  - **Docs**: `SKILL.md` rewritten around the caller-orchestrates recipe (the old all-in-one fallback is gone); `CLAUDE.md` gained the recording-unaware-Operator invariant and "the calling agent is the orchestrator; Windower ships capabilities, not workflows", and its sidecar-per-session note is superseded by the exclusivity invariant; `contracts/cli.md`, `README.md`, and both `sidecar-macos-*` READMEs updated. **`windower-site` `src/data.ts`** fixed for the breaking change (usage string, deleted `--no-record`, both worked examples rewritten as the three-call recipe, both example outputs, the "recording as it goes" blurb, `--unbounded` and `operate abort` copy, transcript location, FAQ, a migration note, `run_operator`'s taxonomy label).
  - **Verification**: `pnpm -r build` all 13 projects · `pnpm turbo run test` **19/19** · core 218 · engine 154 · daemon 22 · operator 101 · cli 215 · mcp-server 53 · Swift **235 (1 skipped, TCC-gated)** · `check-no-screencapturekit.sh` confirms the control binary links no ScreenCaptureKit/ReplayKit.
  - **Accepted breaks**: (1) `OperatorRun.target` is required, so pre-Phase-21 run records on disk no longer parse — `OperatorRunStore.readFile` skips unparseable records rather than crashing, so they silently drop out of `operate list`; bounded, and consistent with the accepted pre-1.0 break. (2) `run_operator`/`windower operate` lost every recording option; removed flags stay registered-but-hidden so they fail with a message naming the flag **and** the `start → operate → stop` recipe rather than commander's "unknown option".
- Phase 19 — Operator (v1.2):
  - **Contracts** were already frozen before implementation (`contracts/operator.md`, `performInput`/`captureFrame` in `contracts/sidecar-protocol.md`, `data-model.md`'s `InputAction`/`OperatorRun`/`OperatorStep`/`SecretRef`/`ModelConfig`, `research.md` §2 rows). Three inconsistencies found during implementation and fixed in the specs: `TimelineEvent.source` was `"human"` in `data-model.md` but `"user"` everywhere else (→ `"user"`); `contracts/operator.md` §Transcript format redefined `OperatorRun`/`OperatorStep` with different field names than `data-model.md` (→ now defers to `data-model.md`, which is the Zod source of truth); the bounds guardrail said "clamped" in a table and "ends the run" in prose (→ reject-before-RPC, matching the exit criterion). Also reconciled the "no network calls anywhere" claim in `CLAUDE.md`/`plan.md` with `spec.md` §7's scoped operator exception.
  - **`packages/core`**: `InputActionSchema` (9 kinds) + `inputActionCoordinates`, `OperatorRun`/`OperatorStep`/`SecretRef`/`ModelConfig` schemas with `parseModelConfig`/`parseSecretRef` (first-colon split, so `openai-compatible:llama3:8b` works), `TimelineEvent.source` defaulting to `"user"` (back-compatible with existing `.events.json`), `OutputManifest.operatorRunPath`, `config.operator` block, `performInput`/`captureFrame` in `protocol/methods.ts` + `sidecar-client.ts`, `INPUT_UNSUPPORTED`/`INPUT_OUT_OF_BOUNDS` error codes, the 4 daemon RPCs + `OPERATOR_RUN_NOT_FOUND`, fake-sidecar implementations of both new methods, and `src/operator/types.ts` — the `OperatorDeps`/`OperatorRunOptions`/`OperatorRunResult`/`ResolvedSecret`/`RunOperator` seam that keeps the daemon and the operator runtime decoupled.
  - **`native/macos`**: `InputSynthesis.swift` (all 9 `InputAction` kinds via `CGEventPost` on a private `CGEventSource`; `type_text` uses `keyboardSetUnicodeString` per grapheme cluster so emoji/CJK work; named-key map covers a–z, 0–9, F1–F20, arrows, editing and punctuation keys) and `FrameCapture.swift` (`SCScreenshotManager` + PNG/JPEG encode + `maxWidth` downscale). Synthetic events are tagged `WindowerEventTag.magic = 0x5749_4E44_4F57_4552` ("WINDOWER") on `.eventSourceUserData`; `EventTapCapture.swift` reads that tag to stamp `source: "operator"` vs `"user"` and stays `.listenOnly`. `describe` advertises `input.mouse`/`input.keyboard`/`screenshot`. `SCScreenshotManager` is macOS 14+ against a 13.0 deployment target, so `screenshot` is advertised statically and throws `UNSUPPORTED_CAPABILITY` at call time on 13.x (same optimistic convention as `eventTimeline.*`).
  - **`packages/operator`** (new): observe → decide → act loop on the Vercel AI SDK (`ai` v7 + `@ai-sdk/anthropic`/`openai`/`openai-compatible`), provider chosen by registry entry from a `provider:model` string — adding a provider is a registry line, zero loop changes. Closed 13-tool surface exactly per `contracts/operator.md`, tools declared **without** `execute` so this package (not the SDK) runs them, which is what keeps guardrails/secrets/recording on one path. Guardrails are runtime-enforced, not prompted: `maxSteps`, wall-clock deadline, per-coordinate bounds check before any RPC, `AbortSignal` checked before every model call and every RPC. Secret substitution happens in exactly one place (the `type_text` handler, immediately before `performInput`). `createRedactor` is a single write barrier over the transcript, every step, every tool result returned to the model, and every log line. Transcript at `<recording>.operator.json` with content-addressed frames in a `<recording>.operator.frames/` sibling dir, written incrementally per step.
  - **`apps/daemon`**: `OperatorRunManager` + `OperatorRunStore` (`~/.windower/operator-runs/<runId>.json`, write-then-rename on every transition so a concurrent `operate status` can't read a torn file), `SecretResolver` (env / keychain behind a swappable resolver interface / literal-with-warning, resolved in-memory per run and never persisted), crash recovery marking in-flight runs `failed` on the same startup path as `recoverCrashedSessions()`, and the 4 RPCs dispatched in `server.ts`. `runOperator` is injected via `loadRunOperator` (default dynamic-imports `@windower/operator` through a non-literal specifier) so the daemon builds and tests standalone.
  - **`packages/cli` / `packages/mcp-server`**: `windower operate` (reusing `addSharedRecordingFlags`, plus `--model`/`--base-url`/`--secret`/`--max-steps`/`--timeout`/`--unbounded`/`--no-record`) and `operate status|abort|list`, all `--json`-capable; no new exit codes per `contracts/cli.md`; no `--api-key` flag anywhere (asserted by test). Three MCP tools (`run_operator`, `get_operator_run`, `abort_operator_run`) at `--json` parity; `list_operator_runs` is deliberately CLI-only per `contracts/mcp-tools.md`. `DaemonClient` gained the four operator wrappers.
  - **Docs**: `plugins/claude-code/SKILL.md` gained a "driving the UI yourself vs. delegating to the operator" section (the two-call flow with the agent's own tools stays the default; operator is the fallback for UI the agent has no tool for); `README.md` notes the operator's provider dep and env-only API key; `windower-site` gained an `#operate` section framing the operator as **optional** — the recommended path is still letting the agent drive with its own tools (Chrome skills / browser tool) and recording around it.
- Phase 20 — Daemon-Optional (v1.3): **code complete**, spec + implementation. Motivated by Phase 19 live testing dying on daemon ergonomics, not operator code — `spawnDaemonDetached` froze `process.env` (and macOS's TCC identity) for the daemon's whole life from whichever shell first won the spawn race.
  - **Contracts**: `contracts/cli.md` gained a Daemon policy section naming the three backend modes (`local` / `daemon` / `attach`) and which commands run in each, replacing the now-false "daemon auto-starts on first use of any other command" claim; `operate` documented as blocking-by-default with `--detach` opt-out; `record` documented as daemon-free with two-stage Ctrl-C (finalize, then cancel). `contracts/mcp-tools.md`: `run_operator` justification for staying non-blocking/daemon-backed written into the contract so it isn't re-opened; new `PermissionReport` fields on `check_permissions`; `shutdown` gained `mode`. New `contracts/daemon-rpc.md`: `hello`, `daemon_info`, `DAEMON_PROTOCOL_VERSION`, `DAEMON_VERSION_MISMATCH`/`DAEMON_BUSY`. `data-model.md`: `PermissionReport` additions (all optional, old daemons still parse) and `RecordingSession.owner`.
  - **`packages/core` primitives**: `src/fs/atomic-write.ts` (`writeFileAtomic`, temp+rename), `src/fs/file-lock.ts` (`FileLock` — `O_EXCL` acquire, pid-liveness stale steal, unlink release), `src/daemon/protocol.ts` (`DAEMON_PROTOCOL_VERSION` + `hello`/`daemon_info` schemas), `src/daemon/state-file.ts` (`~/.windower/daemon.json` written on listen, unlinked on stop), `src/daemon/backend.ts` (`WindowerBackend` interface), `src/daemon/policy.ts` (the one command→`local`/`daemon`/`attach` table, consumed by both CLI and MCP), `packageVersion()` replacing the hardcoded `WINDOWER_VERSION`/`.version("0.0.0")` strings, and new `DAEMON_VERSION_MISMATCH`/`DAEMON_BUSY` error codes with CLI exit-code mapping.
  - **Daemon lifecycle hardening** (`apps/daemon`, `packages/core/src/daemon/connect.ts`): `hello` handshake in `ensureDaemonRunning` (connect → compare protocol versions → auto-restart if idle and safe, at most once; `DAEMON_BUSY` naming active session/run ids if not); back-compat is free because an old daemon rejects the unknown `hello` method with `INVALID_ARGS`. Per-connection `RequestContext` (`{env, cwd, windowerHome}`) threaded into `SecretResolver` and the operator run engine; a detached run snapshots env at start rather than reading the connection later — **this is the actual root-cause fix** for the Phase 19 frozen-environment bug (`OperatorRunOptions.env` now reaches `resolveModel`). Graceful `shutdown({mode})`, default graceful: stop accepting → abort active operator runs → `stopRecording` every in-flight session (video+manifest+events all land) → close/unlink socket and `daemon.json` → exit, bounded ~30s with escalation to immediate; `process.on("exit")` SIGKILL sweep over `activeSidecars` as a backstop. Spawn lockfile `~/.windower/daemon.lock` (only unlink a stale socket when its pid is dead/absent and the connect error was `ECONNREFUSED`). `checkIdle` now counts operator runs, not just sessions. New `windower daemon restart [--force]`. `hello` also cross-checks `windowerHome` between CLI and daemon, closing a silent split-brain.
  - **`@windower/engine`** (new package): moved out of `apps/daemon` largely verbatim — `session-store`, `operator-run-store`, `output-resolver`, `event-timeline-writer`, `narration-mux` (later split out into `@windower/engine-narration`, see "Resolved in a follow-up session" below), `passthrough`, `secret-resolver`, `operator-run-engine` (renamed from `operator-run-manager`), a new extracted `manifest.ts` (`buildManifest`/`writeManifest`), `RecordingEngine` (today's `SessionManager` plus `TargetLock`), and `LocalWindower` implementing `WindowerBackend` over the engine — the daemon-free half of CLI/MCP routing. `apps/daemon` re-exports the moved symbols `@deprecated` for one minor. Existing daemon tests moved with their files, stayed green on import-path edits only (no behavior change, per the phase's acceptance criterion). `recoverCrashedSessions()` stays daemon-only by design — it marks every `recording` session `failed` at startup and must never run from a daemon-free CLI process.
  - **Cross-process safety**: `packages/engine/src/target-lock.ts` (`TargetLock`, file-lock-backed, replaces the in-memory `activeTargetKeys` map so a daemon-free `record` and a daemon-backed `start` on the same display can't both capture; `TARGET_ALREADY_RECORDING` now names the owning pid/session). `SessionStore` gained atomic writes (fixes a torn-read-looks-like-missing-session bug), mtime-based cache invalidation, and an optional `owner: {pid, startedAt}` field so a process only mutates sessions it owns or whose owner is dead — what makes `attach`-mode local `stop`/`cancel` safe.
  - **CLI routing** (`packages/cli`): `src/backend.ts`'s `withBackend(commandId, json, fn)` replaces `withDaemon`, one-line change per command action; `--daemon`/`--no-daemon` flags and `WINDOWER_BACKEND=local|daemon` escape hatches; a test asserts every registered commander command has a policy-table entry. `record` is daemon-free with two-stage SIGINT (finalize, then cancel). `status`/`list`/`operate status`/`operate list` read stores directly; `stop`/`cancel`/`daemon status`/`daemon stop` use `attach` (connect-only, never spawn — safe because a `recording` session always has a daemon owner by construction, so "nothing listening" means the owner is dead and the local fallback marks it `failed`/`canceled`).
  - **`doctor` rewrite**: fully daemon-free — spawns its own transient sidecar, only **probes** for a daemon rather than spawning one; `daemonRunning` is no longer hardcoded `true`. Report gained daemon identity (`{running, pid, version, protocolVersion, startedAt, ageSeconds, socketPath, versionMatchesClient}`), client identity, sidecar resolution source, `windowerHome`, `outputDir` writability, active sessions/runs, and API-key env-var **presence** (never values) for both client and daemon — a `present in CLI: yes / present in daemon: no` row is a one-line self-diagnosis of the exact bug that motivated this phase.
  - **`operate` blocking by default**: runs the operator engine in-process against `LocalWindower`, `onStep` progress to stderr, terminal `OperatorRun` to stdout, Ctrl-C aborts and finalizes the recording; `--detach` restores the daemon-backed `{runId}` shape. `@windower/cli` gained a direct `@windower/operator` dependency but kept the lazy dynamic-import seam.
  - **MCP routing** (`packages/mcp-server`): same policy table; `run_operator` unchanged (non-blocking, daemon-backed, per the contract note above); `check_permissions`/`list_targets`/`resize_window`/`get_session`/`list_sessions` go `local`.
  - **Docs**: `README.md` install/run section leads with `npx windower record` as the zero-setup path; `plugins/claude-code/SKILL.md` documents blocking-vs-`--detach` operator guidance; a new central environment-variable reference covers `WINDOWER_HOME`, `WINDOWER_SIDECAR_BINARY_PATH`, `WINDOWER_DAEMON_BIN_PATH`, `WINDOWER_BACKEND`, `WINDOWER_OPERATOR_DEBUG`, and the provider API-key vars (previously scattered across three files or undocumented). `windower-site` quickstart/CLI/FAQ copy updated to drop daemon-centric phrasing — `record` and `doctor` are framed as daemon-free, `start`/`stop` explicitly called out as the one flow that spins up a background daemon (automatically), and a new FAQ entry answers "do I need to run a daemon?" directly.
  - **Two real bugs found and fixed as side effects**, both surfaced by the doctor-rewrite work: `sidecar-process.ts`'s `terminate()` hung forever on a spawn-time `ENOENT` (it only fires `"error"`, never `"exit"`, in that case) — fixed with a settled-promise pattern that resolves on either event. Unhandled `readline` `"error"` events in `sidecar-client.ts`/`daemon-client.ts` could crash the whole process on abrupt transport teardown — fixed with a no-op listener.
  - **Resolved in a follow-up session**: `@windower/mcp-server`'s transitive `ffmpeg-static` dependency (via `@windower/engine`'s `narration-mux` re-export) is fixed. `narration-mux.ts` moved into a new package, `@windower/engine-narration` (the only workspace package depending on `ffmpeg-static`); `RecordingEngine` now takes `muxNarration`/`validateNarrationFile` as injected constructor options with no default (throws `INTERNAL_ERROR` if narration is requested without them), and `apps/daemon` (the only host that runs narration-muxed recordings) is the one place that imports `@windower/engine-narration` and wires the real implementations in. `pnpm why ffmpeg-static --filter @windower/mcp-server` is now empty. `doctor`'s `presentInDaemon` field for API-key env-var presence is still always `false` (unverifiable) because no RPC lets a client ask a running daemon "do you see env var X" — closing this needs a small new `daemon_info` capability, deliberately left undone since it touches the frozen contract.
- Phases 0–11 (foundation, sidecar protocol, macOS enumeration/permissions, window control, video capture, audio, daemon/session lifecycle, CLI, MCP server, plugin/skill, event timeline, narration hook).
- Phase 12 — Output Management:
  - `apps/daemon/src/output-resolver.ts`: `resolveFilenameTemplate` (`{target}`/`{sessionId}`/`{timestamp}`/`{date}`/`{time}`, sanitized), `ensureWritableOutputDir` (mkdir -p + write-probe, throws `DaemonError("OUTPUT_DIR_NOT_WRITABLE", ...)`), `resolveUniqueOutputPath` (never overwrites, `-2`/`-3`... suffixing).
  - New `OUTPUT_DIR_NOT_WRITABLE` daemon error code (`packages/core/src/daemon/methods.ts`) + CLI exit code mapping (`packages/cli/src/exit-codes.ts`).
  - `apps/daemon/src/session-manager.ts` `startRecording` now reads `~/.windower/config.json` (`readConfig`), merges `defaultVideo`/`defaultAudio` under per-call `params`, resolves `outputDir`, does the writable pre-flight check *before* any session/sidecar state exists, and resolves the final collision-safe output path via the template — all before spawning the sidecar.
  - `stopRecording` now `rename()`s the sidecar's temp output file into the resolved final path (real failure surfaces as a hard error, not swallowed).
  - `packages/core/src/protocol/fake-sidecar.ts`'s `stopCapture` now writes a real (small) temp file so the daemon's real `rename()` has something to move in tests.
  - `manifest.json` writer (already existed pre–Phase 12 work) verified against `OutputManifestSchema` via a Zod-validation test — matches `data-model.md`.
  - `windower config get|set` (Phase 7) already read/wrote `~/.windower/config.json` correctly — no changes needed there.
  - Exit criteria met: writes to configured output folder, manifest matches schema (Zod-validated in test), filename collisions never overwrite, non-writable `outputDir` fails at `start` not `stop`.
- Phase 13 — Testing & Hardening:
  - `fixtures/demo-app`: deterministic-geometry AppKit fixture (SPM, not part of the pnpm/turbo workspace). Fixed window (title, frame, 3 buttons at known content-view coordinates), JSONL click log (`WINDOWER_DEMO_LOG`), `WINDOW_READY` stdout startup signal. Built via `fixtures/demo-app/package-app.sh`, which wraps the executable in a minimal `.app` bundle (`Info.plist`, `CFBundleIdentifier: com.windower.demo-app`) — **required**, not optional: `native/macos`'s `Enumeration.swift` filters out any window whose owning app has an empty `bundleIdentifier` (same rule that filters windower's own window, see `bugs.spec.md` #1), so a bare unbundled `swift build` executable's window never appears in `list_targets`. Verified independently (`osascript` bundle-id query) that the bundled app now reports a real bundle id.
  - `e2e/` package (new pnpm workspace member, own `package.json` with `test:e2e`/`test:soak` scripts — deliberately **not** named `test`, so `pnpm turbo run test`/CI never picks them up): real daemon + real compiled sidecar + demo-app fixture, no fake-sidecar anywhere. Golden-path test (enumerate → resize → start w/ audio → synthetic `osascript`/System Events clicks on all 3 known buttons → stop → assert manifest schema, video resolution/fps via `ffprobe`, event timeline vs. the fixture's own click-log ground truth), a 30-minute soak test (audio/video drift + daemon/sidecar RSS growth + finalization), and two real-process crash-injection tests (`kill -9` the sidecar mid-recording; kill+restart the daemon mid-recording and assert Phase 6 `recoverCrashedSessions` marks it `failed`). Documented as a **required manual pre-merge check** in `e2e/README.md` (TCC grants can't be scripted in CI) — `.github/workflows/ci.yml`'s existing `test` step is untouched.
  - Error-taxonomy coverage: audited all 7 `contracts/sidecar-protocol.md` codes end-to-end (daemon → CLI/MCP output). Found and fixed a real bug: `apps/daemon/src/server.ts`'s `toDaemonError` didn't handle `SidecarError` thrown directly by `PassthroughService` (resize/list_targets/request_permission), silently degrading `RESIZE_UNSUPPORTED`/`PERMISSION_DENIED`/`UNSUPPORTED_CAPABILITY` to `INTERNAL_ERROR` before they reached callers. Added `PERMISSION_DENIED` simulation to the fake sidecar (resize/capture/mic gated paths) and a new CLI exit code (`EXIT_PERMISSION_DENIED = 5`). All 7 codes now have a CLI- or MCP-level propagation test.
  - CI strategy: unchanged `test` step covers everything below the TCC line (protocol, schemas, CLI/MCP parsing, daemon logic against the fake sidecar — `pnpm turbo run test`, 13/13 tasks green). `pnpm test:e2e` / `pnpm test:soak` from repo root run the real-sidecar suite locally; see `e2e/README.md` for TCC-grant prerequisites.
- Full monorepo build+test green (`pnpm turbo run build`, `pnpm turbo run test`, 13/13 tasks): `@windower/core`, `@windower/daemon`, `@windower/cli`, `@windower/mcp-server`, `@windower/sidecar-macos` (Swift), `@windower/e2e` (build/typecheck only, per above).

## Live verification results (Phase 21, run this session)

Manual, TCC-gated checklist from `tasks/phase-21-capture-control-broker.md` §"Live verification", run on real hardware (macOS 15.7.3, `windower doctor` confirming Screen Recording / Accessibility / Microphone all `granted`, capture lock free, cold start with no daemon pre-started) against `anthropic:claude-sonnet-5`. **6 of 12 items PASS; 6 NOT RUN (blocked on Anthropic credits, see below). 0 FAIL.**

**Blocker found and fixed before verification could start (this is `bugs.spec.md` #10, and it is a pre-existing Phase 20 bug, not a Phase 21 regression):** the very first caller-driven attempt died in 2s with `OPERATOR_MISSING_API_KEY` and 0 steps, despite the calling shell *and* the daemon process both having `ANTHROPIC_API_KEY` (verified with `ps eww` on the daemon pid). `packages/cli/src/backend.ts:72` called `ensureDaemonRunning()` with no options → `hello` carried `env: undefined` → `buildRequestContext` produced `env: {}` → `operator-run-engine.ts:283`'s `context ? {...context.env} : undefined` saw a *truthy* empty object and passed it down, so `resolveModel`'s `env = process.env` default never applied and the daemon's own good environment was shadowed. `packages/mcp-server` already did this correctly; the CLI had no equivalent. Fixed by lifting `buildOperatorHelloEnv` into `packages/core/src/daemon/operator-env.ts` (mcp-server's module is now a one-line re-export, so there is one implementation), plumbing an optional `env` through `withBackend`, and making an empty snapshot mean "nothing scoped" rather than "empty environment". +10 tests closing the seam, including a `request-context.test.ts` that did not exist and a CLI test driven through the real command registration/parse. Why it escaped: both halves of the contract were tested in isolation and the seam between them was not — the same class of miss as the earlier commander `--json` parse-layer bug. Phase 20's own live verification only exercised *blocking* `operate` (local, in-process, never sends `hello`), so the `--detach` path — which Phase 21's caller-orchestrated recipe requires — had never been run live.

### PASS (6)

1. **Core repro, 3 runs — the load-bearing item.** Caller-driven `start_recording` → `operate --detach` → poll `operate status` → `stop_recording`, display target, Finder→Safari→waroom.co, `--max-steps 30`, model `anthropic:claude-sonnet-5`, no daemon pre-started, `log stream` traced on **all three** runs (not just one). `video.durationMs` is asset-derived and was independently re-confirmed with `ffprobe` on every run:

   | Run | Wall clock | Manifest video | `ffprobe` | Preserved | Steps | Terminal state |
   |---|---|---|---|---|---|---|
   | 1 | 304,273 ms | 304,011.67 ms | 304,012 ms | **99.9%** | 29 | `timed_out` (300s guardrail) |
   | 2 | 211,285 ms | 211,101.67 ms | 211,102 ms | **99.9%** | 30 | `failed` — `OPERATOR_MAX_STEPS_EXCEEDED` |
   | 3 | 273,304 ms | 273,138.33 ms | 273,138 ms | **99.9%** | 29 | `failed` — `OPERATOR_MODEL_ERROR` (credits exhausted) |

   Against the historical baselines for this bug (2.7%, 59.5%, 81.2%, ~101%), three consecutive 99.9% runs is the first time the repro has been *durable* rather than lucky. Note the three terminal states are all **guardrail or account outcomes, not capture failures** — every one of them is the operator loop ending for its own reasons while the recording kept perfect fidelity underneath, which is itself the independence the phase is about. Runs 2–3 used `--timeout 420` after run 1 hit the 300s default.
2. **`log stream` tracing — zero replayd invalidation.** **Zero `didStopWithError` lines across all three traces** (run 1's trace alone is 2,907 lines over 5.5 minutes). Each run shows exactly one `RPClientProxy streamDidStartWithConfiguration` → `streamDidStopWithConfiguration` pair bracketing the session, and the only `invalidated because the client process (pid N) either cancelled the connection or exited` lines are each process's *own* exit. Crucially, the trace shows **the mutex serializing the two capture processes rather than letting them overlap**: `start`'s transient target-resolution sidecar and the recording's sidecar never coexist — run 1: pid 8527's last line 16:59:22.072, pid 8529 starts .125, stream starts .254; run 2: 29484 ends 17:05:50.605, 29485 starts .615, stream starts .777; run 3: 44016 ends 17:10:05.301, 44017 starts .311, stream starts .468. This is the positive evidence the phase asked for — absence of errors would not have been the same thing.
3. **Exactly one capture process machine-wide** (organic load). A `ps` sampler running every 3s across all three runs never observed more than `capture=1 control=1 loop=1`, and the counts move in the expected order (capture → control → loop child, then loop → control → capture on teardown).
4. **Busy-mutex behavior.** With a daemon-free `windower record` holding `~/.windower/capture.lock`, a second daemon-free capture call (`windower targets`) waited the bounded budget and returned `SCREEN_CAPTURE_BUSY`: *"Screen capture is owned by pid 69408 (acquired …) and did not become available within 2000ms (waited 2082ms)."* Exactly one capture process existed before, during, and after the refused call — **no second SCK process was spawned and the caller was not routed into the holder**. The lock payload on disk was exactly `{ pid, acquiredAt, windowerHome }`, i.e. it has not grown a child pid or a session id. Lock file unlinked on the holder's exit.
5. **Mutex stale-holder recovery + process ownership.** `kill -9` on the lock *owner* (the `record` CLI process): the `windower-capture-macos` child **did not survive** — confirming stdin-EOF process ownership works with no pid tracking, reaper, or supervisor. The stale lock file remained with a dead pid, and the very next daemon-free `list_targets` stole it cleanly in **230ms**, returning 35 targets; a second call also succeeded, proving the capture surface was not wedged.
6. **Recording-without-operator parity + resource hygiene.** A `start_recording`/`stop_recording` pair around 45s of ordinary `osascript`-driven desktop activity (no operator involved at all) produced a session record and manifest whose **key sets are identical** to all three caller-driven runs (programmatic recursive key-set diff, both files, all three comparisons clean), and a grep of both for `operator|runId|loop-entry` found **nothing** — the record does not identify what drove the screen. Its event timeline carried `{"user": 2}` and zero `operator`-tagged events. Afterwards: no surviving `windower-capture-macos` / `windower-control-macos` / `loop-entry.js` processes, and no `~/.windower/capture.lock`.

Also observed, per the phase's "worth watching" note: the operator produced a **revision-0 plan before any input tool** on every run, and emitted model-stated checkpoints (3 on run 1, 2 on run 2, 0 on run 3) — i.e. `plan → execute → observe → checkpoint` is behaving as specified, and no checkpoint outcome was inferred by the runtime.

### NOT RUN (6) — blocked on Anthropic credits

Run 3 ended with `OPERATOR_MODEL_ERROR: "Your credit balance is too low to access the Anthropic API."` Every remaining item needs a live operator run in flight, so all six are outstanding. **The product owner's decision this session was to finish the model-free items and stop, rather than switch providers** (a different provider would no longer be comparable to every prior bug-#6 baseline). Harness scripts for all six are written, syntax-checked, and reviewed, ready to run as-is once credits exist — see "Next session — start here".

1. **Concurrent-load stress variant** — a synthetic `list_targets` burst at faster-than-organic cadence layered on a real caller-driven run, to prove exactly-one-capture-process under load heavier than a model generates. *(`stress-run.sh`)*
2. **Crash injection — control surface** (`kill -9 windower-control-macos` mid-run). *(`crash-control.sh`)*
3. **Crash injection — operator loop child** (`kill -9` the `loop-entry.js` child; expect `OPERATOR_LOOP_CRASHED` with the recording untouched). *(`crash-loop-child.sh`)*
4. **Crash injection — capture sidecar** (`kill -9` mid-recording with an operator run in flight). *(`crash-capture.sh`)*
5. **Operator-without-recording parity** — the identical `run_operator` call with no recording active. *(`parity-and-lifecycle.sh` part a)*
6. **Independent lifecycle** — `stop_recording` delayed well past the run's terminal state. *(`parity-and-lifecycle.sh` part c)*

Code-reading evidence exists for some of these but is **explicitly not a substitute**: `operator-loop-host.ts:992-1024` does end a dead child as `OPERATOR_LOOP_CRASHED` with no recording branch in the crash path, and `control-engine.ts:167-192` surfaces a control-process death as `INTERNAL_ERROR` with a message naming the situation and then respawns on the next call. Two notes for whoever runs these: a control-surface crash currently surfaces as the **generic `INTERNAL_ERROR`** rather than a distinct code — the message is clear, but the phase criterion says "clear, distinct error", so decide whether that satisfies it; and a grep for any path from an operator terminal state to `stopRecording`/session mutation found none.

### Other findings this session

- **Exit criterion "nothing named broker": code is clean, prose is not.** A repo-wide `grep -ril broker` hits only `STATUS.md`, `tasks/INDEX.md`, and three Phase 21 task/prompt docs — all historical narrative describing the reverts. No component, type, error code, or identifier is named broker. However the phase's own task file is still literally named **`tasks/phase-21-capture-control-broker.md`**, which the criterion's "file" wording arguably catches. Not renamed here (it would break cross-references in `INDEX.md`, both prompt docs, and `STATUS.md` mid-verification) — flagged as a deliberate call for the owner.
- `native/macos/scripts/check-no-screencapturekit.sh` re-run and passing: *"windower-control-macos links no ScreenCaptureKit/ReplayKit; windower-capture-macos links ScreenCaptureKit."*
- Suites after this session's fix: `pnpm turbo run test` **19/19** — core 218 · engine **161** (was 154) · daemon 22 · operator 101 · cli **218** (was 215) · mcp-server 53.
- The residual **PTS-gap-stall** pattern did **not** appear in any of the three runs (99.9% preserved leaves no room for it). That is not evidence it is fixed — it correlates with dynamic on-screen content and these runs may simply not have provoked it. Status unchanged: separate, out of scope for Phase 21, still open on #6's residual thread.
- `windower doctor` reported **55 active sessions** at the start of this session — accumulated stale records in `~/.windower/sessions/` from months of testing. Cosmetic, but `doctor`'s "active sessions" number is currently useless as a signal. Worth a cleanup pass or a staleness filter.

## Live verification results (Phase 20, run this session)

Manual, TCC-gated checklist from `tasks/phase-20-daemon-optional.md`, run on real hardware (macOS, real Screen Recording/Accessibility/Microphone grants all confirmed `granted` via `windower doctor`) against a real Anthropic model. All 6 items **PASS**:

1. **Real `windower operate` run** — target: TextEdit document, task included a click + `type_text` with a `--secret` placeholder. `state: "succeeded"`; synthetic click/type landed; `.events.json` tagged every synthetic event `source: "operator"`; `manifest.json`'s `operatorRunPath` pointed at a well-formed `.operator.json` transcript; the secret value never appeared in transcript/manifest/events/stderr — only the `{{name}}` placeholder did. Bonus: the model correctly detected and refused an unrelated prompt-injection attempt visible on screen during the run (not something this test set up — an incidental, encouraging guardrail signal). Caveat: `operate` always records the whole primary display, so any sensitive on-screen content — not just Windower's own redacted transcript — ends up in the video; not a Windower bug, just worth remembering before sharing an `operate` recording.
2. **`windower record --duration 10` with no daemon running** — recorded successfully; `windower doctor` and `ps aux` both confirmed no daemon before or after.
3. **`windower daemon stop` mid-recording** — session landed `finalized` with a valid, `ffprobe`-decodable video (duration matched the manifest, proving no truncation) plus manifest and event timeline; no orphaned sidecar process.
4. **Target-lock contention** — a daemon-backed `start` against a display already being recorded by a concurrent `record` was refused with `TARGET_ALREADY_RECORDING`, naming the owning session id and pid.
5. **Version-mismatch handling** — built a real (not fake-fixture) older daemon in an isolated git worktree with `DAEMON_PROTOCOL_VERSION` set to 0, driven via `WINDOWER_DAEMON_BIN_PATH`. Idle case: client auto-restarted it once to a matching-version daemon and the command succeeded. Busy case: with a live recording session on the mismatched daemon, the client refused with `DAEMON_BUSY` naming the exact session id, per contract — never a silent restart, never a loop.
6. **Blocking `operate` end-to-end + mid-run Ctrl-C** — `SIGINT` mid-run produced `state: "aborted"` (`OPERATOR_ABORTED`) for the operator run itself, but the underlying recording session correctly landed `finalized` (not canceled), with a valid decodable video and manifest.

Three real bugs found and logged during this pass (none fixed, none blocking for Phase 20's own scoped criteria — all pre-existing capture/operator-layer issues surfaced by real, longer live testing): `specs/001-windower-mvp/bugs.spec.md` #8 — `connect.test.ts`'s `ensureDaemonRunning` tests leak real spawned fixture-daemon processes forever (found ~80+ orphaned `fake-daemon-cli.mjs` processes on this dev machine). #9 — a follow-up real `operate` run (Safari via Finder → waroom.co, 28 steps) crashed with `OPERATOR_MODEL_ERROR` from a duplicate `toolCallId` across two tool-result blocks in one message, which the Anthropic API hard-rejects; `packages/operator/src/run.ts`'s tool-result-building loop has no dedup on `toolCallId`. **#6 (updated, previously unconfirmed) — now reproduced with hard evidence and is the most important open item in `bugs.spec.md`:** that same Safari run had a real 201s session with 72 real synthetic input events spanning the full duration (Safari genuinely opened and was interacted with), but the asset-derived video duration was only ~9s — ~192 seconds of genuine on-screen activity silently never made it into the recorded file, with zero errors surfaced anywhere. The existing dropped-frame/stall diagnostics (added in a prior session) have a blind spot: they only detect gaps *between* frames, not a stream that stops delivering frames entirely and never resumes. This is a correctness bug, not a diagnostics gap — recordings can silently truncate while reporting full success. Root cause and suggested fixes for all three are in the bug writeups.

**Separate, unrelated finding (not a Windower bug, an operational hazard from this session's own testing):** the operator's first `captureFrame` screenshot in that same run captured this terminal frontmost with a real `ANTHROPIC_API_KEY` visible in plaintext in an executed shell command — that key was both sent to Anthropic as image content and saved to disk in a frame PNG, in addition to having been pasted directly into the chat transcript twice earlier in the session. The user has been advised to rotate/revoke that key.

## Blocked / not started
- **Phase 21's six unrun live-verification items — blocked on Anthropic API credits.** The phase itself is marked done; these items are carried forward rather than blocking it. The core repro (3/3 runs at 99.9% preserved, zero replayd invalidations) and the mutex/parity/hygiene items passed this session; crash-injection ×3, concurrent-load stress, operator-without-recording parity, and independent lifecycle all need a live operator run and could not be started. Harness scripts written, reviewed, and checked in at `e2e/manual/phase-21/`. **`bugs.spec.md` #6 stays open until they run** — nominal-condition fidelity is proven, failure-isolation is not.
- Phase 14 — Packaging.
- Phase 15+ — v1.1 post-processing, post-MVP Windows/Linux backends.
- **Phase 22 — Operator: AX-first observation and the planner/executor split (v1.5).** Code complete and green (see "Current phase" above); **all five live-verification items are unrun**, gated on TCC + a funded model account, and must be done manually by the user per `e2e/README.md` convention — not this or any subagent's scope.
- `bugs.spec.md` #8 — leaked fixture-daemon processes from `connect.test.ts`/`doctor.test.ts`, found during this session's live verification. Not fixed yet.
- `bugs.spec.md` #9 — `@windower/operator`'s tool-result loop can crash a real run with `OPERATOR_MODEL_ERROR` on a duplicate `toolCallId`, found on a real long-ish `operate` run this session. Not fixed yet — worth fixing before `operate` is treated as reliable for longer tasks.

## Next session — start here

1. **First: put credits on the Anthropic account** (or export a funded `ANTHROPIC_API_KEY` in the running shell). Everything below item 1 is blocked on it, and nothing else in the repo is. Confirm with `windower doctor`'s API-key-presence row *and* one real cheap model call before starting a 4-minute run — run 3 this session burned a full run discovering the balance was empty.
2. **Clear Phase 21's 6 carried-over live-verification items** (the phase is closed; these are debt against `bugs.spec.md` #6, not against the phase). The core repro is done and passed 3/3; do **not** re-run it unless something changes. The remaining six, with ready-to-run harness scripts already written, syntax-checked, and reviewed (they follow `core-repro.sh`'s known-good conventions, print `CHECK <name>: PASS|FAIL` lines, and trap-clean any recording they start): concurrent-load stress (`stress-run.sh`), crash injection ×3 (`crash-control.sh`, `crash-loop-child.sh`, `crash-capture.sh`), and operator-without-recording parity + independent lifecycle (`parity-and-lifecycle.sh`). They are checked in at **`e2e/manual/phase-21/`** (with a README covering prerequisites, the `log stream` tracing recipe, and which have actually been run) — deliberately not wired into `pnpm test`/`test:e2e`/Turbo, same manual/TCC-gated convention as the rest of `e2e/`. While there, settle whether a control-surface crash surfacing as the generic `INTERNAL_ERROR` (rather than a distinct code) satisfies that item's "clear, distinct error" wording.
3. **Only then** write `bugs.spec.md` #6's closing entry, cross-referencing Phase 21. Do not close it on unit tests alone, and do not close it on this session's core repro alone — three clean runs prove the truncation is gone under nominal conditions, but the crash-injection items are what prove a failure in one capability cannot silently truncate another's recording, which is the other half of what #6 has always been about.
3. The residual **PTS-gap-stall** truncation pattern (~13–19%, self-recovering, correlates with dynamic on-screen content, not with any RPC) is a **different, likely OS-level** mechanism and is explicitly **out of scope** for Phase 21. Don't conflate a stall-class regression with the replayd-conflict class this phase eliminates.
4. **Phase 22 is implemented, not yet live-verified.** Code and docs are done (see "Current phase" above). What's actually left is the five-item manual/TCC-gated live-verification checklist in `tasks/phase-22-operator-ax-first.md` — put credits on the account (same prerequisite as item 1 above), then run the motivating waroom.co task with a strong planner + cheap executor and record real steps/model-calls/frames against the 29/30/29 baseline below, plus the other four live items (recording-independence parity, vision-fallback engagement, stale-ref recovery, single-model-parity live).

   **Baselines Phase 22 is measured against, recorded here so they survive this session:** the three Phase 21 live runs at `--max-steps 30` consumed **29 / 30 / 29** steps, one full-strength model call per step, each carrying a fresh 1280px PNG (pre-Phase-22 `run.ts` captured unconditionally, every step). Steps were the binding constraint in all three. Model-calls-per-run equalled steps-per-run then, since the loop made exactly one `generateText` call per step regardless of tier. No token counts were captured — `generateText`'s `usage` is discarded — so there is no per-run token or dollar baseline, and Phase 22 does not add one. **Phase 22's own numbers (steps, planner vs. executor model calls, frames captured, wall-clock) are not yet recorded — that is exactly what the live waroom.co run above must produce.**

## Notes for next session
- **The items below (Phase 19 live-testing debt, the daemon environment-freezing bug) are now resolved** — see "Live verification results" above. Left in place as historical record of what Phase 20 was fixing.
- **Live testing of Phase 19 got as far as the sidecar layer and stopped at the daemon.** Verified working against the real binary: `describe` advertising `input.mouse`/`input.keyboard`/`screenshot`; `captureFrame` (320x208 PNG at maxWidth 320, aspect + downscale correct); `performInput` moving the cursor, with the pixels→points conversion proven correct on a scale-2 display (sent 1500,980 px → cursor landed at 750,490 pt); `INPUT_OUT_OF_BOUNDS` rejecting an off-screen coordinate. One trap worth knowing: the sidecar exits on stdin EOF, and a posted `CGEvent` can be lost if the process dies before delivery — keep the process alive (e.g. a trailing `sleep`) when smoke-testing `performInput` by piping a single line, or it looks like a silent no-op.
- Three `operate` runs failed at the daemon, not the operator: `PERMISSION_DENIED` (daemon spawned by a shell without Screen Recording) and `OPERATOR_MISSING_API_KEY` twice (daemon spawned by a shell without the key). The daemon inherits both the environment and the TCC identity of whichever process first spawns it, and freezes them. **This is what Phase 20 exists to fix.** Until it lands, the workaround is: `windower daemon stop`, then run from a shell that has both the API key exported and the TCC grants.
- Fixed this session: `operate status|abort|list --json` printed human output. Commander binds a flag to the **parent** when parent and subcommand declare the same option, so the subcommands' `opts.json` was always `undefined`. Fixed via `jsonFlag` in `packages/cli/src/commands/operate.ts` with a regression test that exercises the real parent+child registration in one parse. Worth remembering as a class of bug: the existing tests asserted on the param builder in isolation, which cannot catch a parse-layer problem.
- **Phase 19 is unit-green but unproven end-to-end.** Every layer is tested in isolation (core schemas, fake sidecar, stubbed `runOperator` in the daemon, a mock language model in `packages/operator`), but no run has gone real-sidecar → real `CGEventPost` → real LLM. Before calling Phase 19 done: run `windower operate` against a real model with Screen Recording + Accessibility granted, confirm synthetic clicks land, confirm the recorded `.events.json` actually carries `source: "operator"` for them (the tag round-trip through `eventSourceUserData` is the part most likely to be wrong in practice), and confirm the manifest's `operatorRunPath` points at a well-formed transcript.
- `packages/operator` uses AI SDK v7 — observation frames are attached as `{type: "file", mediaType: "image/png"}` because the `image` content part is deprecated there. Worth re-checking on the next SDK bump.
- Secure event input (password fields) blocks the event **tap** but not `CGEventPost` — operator typing into a password field works but is invisible in the timeline. Expected, documented in `contracts/sidecar-protocol.md`; don't chase it as a bug.
- `e2e/` golden-path/crash suites have not been run to a full green in this sandboxed environment (TCC grants for this shell's own ancestry are uncertain/partial here — a `Sidecar stream closed` error surfaced on one manual run after the demo-app bundling fix, distinct from the bundling bug and not yet root-caused; the bundling fix itself was verified independently via a direct `osascript` bundle-id check). Re-run `pnpm test:e2e` from a terminal with confirmed Screen Recording/Accessibility/Microphone grants before treating the e2e suite as proven end-to-end.
