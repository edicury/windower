# Known Bugs / Live Testing Findings

Tracked during manual live testing (Phase 7 CLI). Not spec — a running log to fix later.

## 1. `windower targets` lists many phantom "Desktop" window entries

**Found:** Phase 7 live test, macOS sidecar, `windower targets --json`.

**Symptom:** Output includes dozens of `kind: "window"` entries with `title: "Desktop"`, empty `appName`/`appBundleId`, and `bounds` matching a full external-display resolution (e.g. `7680x2160`) — far more than the number of real on-screen windows. Likely one phantom entry per virtual desktop/Space (or per display arrangement) rather than actual user windows.

**Impact:** Noisy `targets` output; agents/users must filter these out manually to find real windows. Not currently blocking (real windows still resolve correctly), but hurts target discovery UX.

**Suspected source:** `native/macos` window enumeration (Phase 2, `CGWindowListCopyWindowInfo` or similar) likely picking up desktop/wallpaper layer windows per Space that aren't real app windows.

**Fix:** `native/macos/Sources/WindowerSidecarCore/Enumeration.swift` `mapWindow` now also skips windows whose `owningApplication` has an empty `applicationName` or `bundleIdentifier` — the phantom entries had a title but no real app identity. Real Finder desktop window (one instance, `appName: "Finder"`) still surfaces correctly.

**Verified:** live-tested with `windower targets --json` after `swift build` — window count on this machine dropped from ~65 (many empty-app "Desktop" duplicates) to 27 real windows, all with non-empty `appName`. `swift test` — 100/100 pass.

**Status:** Closed.

## 2. Narration mux failures are silently swallowed with no logging

**Found:** Phase 11 live test, real macOS sidecar recording + real `say`-generated AIFF narration muxed via `stop_recording({ narration })`.

**Symptom:** On the first live-test attempt, `stop_recording` returned a normal-looking success result: `outputPath`, `manifestPath`, and a full `manifest`. But `manifest.narration` was completely absent (both in the RPC result and in `manifest.json` on disk), and `ffmpeg -i <outputPath>` showed only the original video stream — no narration audio track had been muxed in at all. No error was thrown or surfaced anywhere in the MCP tool result.

**Root cause (this occurrence):** environmental, not a logic bug in `narration-mux.ts` itself — the long-lived daemon process had been started ~11 minutes *before* `apps/daemon/dist/session-manager.js`/`narration-mux.js` were rebuilt with the narration feature, so it was serving stale in-memory code. Killing the stale daemon (and the two stale MCP-server stdio processes whose memoized `DaemonClient` connections died with it) and letting them respawn against the current build fixed it; a clean rerun produced a correct mux (1 video + 1 AAC audio stream, video untouched, `manifest.narration` present with `trackIndex`).

**Real gap exposed:** `apps/daemon/src/session-manager.ts` (`stopRecording`, ~lines 281-300) deliberately swallows any `muxNarration` failure with an empty `catch {}` so a mux problem doesn't fail an otherwise-good recording — that's a reasonable product choice, documented in `narration-mux.ts`'s file-header comment. But nothing is logged when it happens. Any future mux failure (bad ffmpeg-static binary, disk full, permissions, a real regression) will look identical to "narration wasn't requested" from the caller's point of view — a video with no narration and no `manifest.narration`, no diagnostic. Same pattern also applies to the adjacent `EventTimelineWriter.finalize()` swallow a few lines above.

**Suggested fix:** log the caught error (daemon's logger, whatever that is) before continuing, in both swallow sites, so silent failures are at least debuggable. Not yet fixed — not addressed by this test.

**Status:** Fixed. `apps/daemon/src/session-manager.ts`'s `stopRecording` now logs both swallowed errors via `console.error` (the only logging convention present in `apps/daemon`, matching `bin.ts`) before continuing: `[SessionManager] event timeline finalize failed for session <id>: <error>` and `[SessionManager] narration mux failed for session <id>: <error>`. Swallow-and-continue behavior is unchanged; only logging was added.

## 3. MCP server's memoized `DaemonClient` doesn't self-heal after the daemon dies mid-session

**Found:** Same Phase 11 live test, while investigating bug #2 above — after killing the stale daemon process to force a fresh respawn.

**Symptom:** Once the daemon process was killed, the next `mcp__windower__*` tool call failed with `[DAEMON_UNREACHABLE] DaemonClient is disposed`, and every subsequent call kept failing the same way — it never recovered on its own.

**Root cause:** `packages/mcp-server/src/daemon-client.ts`'s `getDaemonClient()` memoizes one `DaemonClient` promise for the process's whole lifetime, and only clears that memoized promise (`clientPromise = undefined`) in the `.catch()` of the *initial connect* — i.e. if `ensureDaemonRunning()` itself rejects. It has no path to recover once a previously-successful connection later gets disposed (e.g. the underlying daemon process dies): the disposed client just keeps getting returned and erroring forever, for the life of the MCP server process.

**Impact:** Any daemon crash/restart while an MCP server (Claude Code plugin, dev server, etc.) is connected permanently breaks that MCP server's ability to reach the daemon until the MCP server process itself is restarted. In this test, restarting the MCP server process (killing `packages/mcp-server/dist/index.js`) let the host respawn it and reconnect cleanly — no code fix required to work around it, but nothing in the code does this automatically.

**Suggested fix:** in `getDaemonClient()`, detect a disposed/unreachable client at call time (or catch `DAEMON_UNREACHABLE`-style errors from `client.call`) and clear `clientPromise` so the next call reconnects, mirroring the existing initial-connect retry behavior.

**Status:** Fixed. `DaemonClient` (`packages/core/src/daemon/client.ts`) now exposes a public `isDisposed` getter over its existing private `disposed` flag. `packages/mcp-server/src/daemon-client.ts`'s `getDaemonClient()` checks `isDisposed` on the resolved client; if the memoized connection has died, it drops `clientPromise` and reconnects (recursively, so the same call transparently gets a fresh client instead of only healing on the *next* call). Covered by a new test in `packages/mcp-server/src/daemon-client.test.ts`.

## 4. `startCapture` against a `window`-kind target crashes the sidecar (`CGS_REQUIRE_INIT` assertion)

**Found:** Phase 18 live test, macOS sidecar, dogfood-recording the windower-site marketing demo via `start_recording` targeting a real Chrome window (`{kind: "window", id: "138", ...}` from `list_targets`).

**Symptom:** `start_recording` (and the daemon's underlying `startCapture` call) fails immediately with `[INTERNAL_ERROR] Sidecar stream closed` — no structured `SidecarError`, just the transport dying. Reproduced directly against the compiled sidecar binary outside the daemon (feeding raw JSON-RPC over stdio): the process aborts with `Assertion failed: (did_initialize), function CGS_REQUIRE_INIT, file CGInitialization.c, line 44.` immediately after accepting a `startCapture` call whose `target.kind === "window"`.

**Isolation:** the exact same `startCapture` params, only with `target.kind` changed to `"display"` or `"region"` (same underlying display, region bounds matching the window's on-screen position), succeed cleanly — `{"result":{"started":true}}`. The crash is specific to the window-capture code path, not capture in general.

**Impact:** `capture.window` (a capability the sidecar advertises in `describe`) is completely broken — any caller (CLI `--kind window`, MCP `start_recording` with a window target, the Claude Code skill's normal target-selection flow) crashes the whole sidecar process instead of recording. Workaround used for Phase 18: record a `region` target with bounds matching the window's position/size instead of the window itself (loses automatic tracking if the window later moves, but captures the same pixels).

**Suspected source:** `native/macos/Sources/WindowerSidecarCore/CaptureService.swift`'s window-target branch of `SCContentFilter`/`SCStreamConfiguration` resolution (Phase 4) — something in that path touches a Core Graphics / window-server API that requires an initialized `CGS` connection this process doesn't have (plausibly an `AXUIElement`-adjacent or `CGWindowListCreateImage`-style call used only for window (not display/region) targets), and the resulting assertion aborts the whole process instead of throwing a catchable Swift error.

**Fix:** `native/macos/Sources/WindowerSidecarCore/CaptureService.swift`'s `startCapture(params:)` `"window"` case (~line 428) no longer calls `SCContentFilter(desktopIndependentWindow:)` — that initializer is confirmed to require a CGS/WindowServer session this headless stdio process never establishes, and was the sole source of the crash. It now resolves the `SCDisplay` the target window is on (new `resolveWindowDisplay(_:in:)` helper, ~line 801: matches by point-containment of the window's origin against `content.displays`, falls back to frame intersection, then to the first enumerated display, never throws unless there are zero displays) and builds the filter via `SCContentFilter(display:including:)` — the same initializer the `"display"`/`"region"` branches already use safely — restricted to just the target window. The capture is then cropped to the window's bounds using the same `sourceRect`/`destinationRect` pattern the `"region"` branch already uses (`CaptureConfigService.regionSourceRect`, converting the window's pixel bounds to display-relative points). `nativeWidth`/`nativeHeight` computation is unchanged. A comment at the call site documents why `desktopIndependentWindow` isn't used, so it isn't reintroduced without fixing the underlying CGS-init problem first. Known trade-off (documented in the same comment): unlike `desktopIndependentWindow`, capture no longer automatically follows the window if it's moved to another Space mid-recording.

**Verified:**
- `swift build` — clean.
- `swift test` — 118/118 pass (same count as before the fix).
- Direct binary test: fed raw JSON-RPC `startCapture`/`stopCapture` over stdio to the compiled `native/macos/.build/debug/windower-sidecar-macos` targeting a real on-screen window (`id: "138"`, Chrome, obtained via the sidecar's own `enumerateTargets`). No crash/assertion (confirmed both via a bare `startCapture`-only call against an off-Space window, which now returns a catchable `CAPTURE_FAILED` JSON-RPC error instead of aborting the process, and via a full start/stop pass against an on-screen window). `startCapture` returned `{"started":true}`, `stopCapture` returned a normal result, and the written file (`ffprobe`'d via the repo's vendored `ffprobe-static` darwin/arm64 binary) was a valid non-empty H.264 mp4 at the window's exact pixel bounds (3024x1718).
- Full MCP/daemon/sidecar path: killed the stale daemon process so the MCP server would respawn it against the freshly built sidecar, then called `mcp__windower__start_recording` with a real `{kind: "window", id: "138", ...}` target from `list_targets`, waited a few seconds, then `mcp__windower__stop_recording`. No `[INTERNAL_ERROR] Sidecar stream closed`; `stop_recording` returned a finalized `outputPath`/`manifestPath`/`manifest`, and the finalized mp4 (`ffprobe`'d) was a valid ~10.3s H.264 video at 3024x1718.

**Status:** Fixed.

## 5. Terminal-footage recording: `manifest.durationMs` doesn't match actual decodable video length

**Found:** Phase 18 live test, recording a region matching a terminal window while running real `windower` CLI commands.

**Symptom:** `stop_recording`'s manifest reported `video.durationMs: 17213.49` (~17.2s) for session `cbaf26ca-2cc1-4301-b9a8-254e3ddc277f`, but `ffprobe`/a full decode pass of the resulting mp4 (`4-2026-08-09T14-18-17.mp4`) showed only ~7.7-7.9s of actual decodable frames (138 frames at the recorded fps) — under half the claimed duration.

**Impact:** Any caller trusting `manifest.video.durationMs` (trim/post-processing tooling, UI duration displays, Phase 15's future auto-zoom/trim work) will be wrong by a large margin for at least some recordings — the file itself is shorter than claimed, not the other way around, so trimming logic that assumes the manifest duration is authoritative could seek past the end of real content.

**Suspected source:** Not investigated — could be `VideoAssetWriter`/`AVAssetWriter` finalization on the Swift side reporting a stale/wall-clock session duration instead of actual muxed-frame duration, or frames being dropped/not delivered to the writer for a long stretch without that being reflected in what's reported to the daemon at `stopCapture` time. Same category of bug as #6 below (frames not actually landing despite the capture session reporting healthy) — may share a root cause, worth investigating together.

**Fix:** Root cause confirmed: `native/macos/Sources/WindowerSidecarCore/CaptureService.swift`'s `stopCapture` computed `actualDurationMs` purely from wall-clock elapsed time (`Date().timeIntervalSince(context.startedAt) * 1000`), with no relation to how much content actually landed in the file — `VideoAssetWriter` never tracked frame timestamps or asset duration. Fixed in `native/macos/Sources/WindowerSidecarCore/VideoAssetWriter.swift`: `finish(completion:)` (line ~179) now, after `AVAssetWriter.finishWriting` reports `.completed`, loads the finished file via `AVURLAsset(url: outputURL).load(.duration)` (bridged into the existing completion-handler API via `Task { }`) and hands the real duration back to the caller in milliseconds through a new `Self.milliseconds(from:)` pure conversion helper (`CMTime` → `Double?`, `nil` for a non-numeric time). The completion signature changed from `Result<Void, Error>` to `Result<Double?, Error>` — `nil` on success means the write succeeded but the duration couldn't be read (corrupt/empty asset), which the caller treats as a fallback case, not a hard failure. `native/macos/Sources/WindowerSidecarCore/CaptureService.swift`'s `stopCapture` (line ~729) now keeps the wall-clock computation as `wallClockDurationMs` (diagnostic/fallback only, never reported directly) and uses `realDurationMs ?? wallClockDurationMs` as `actualDurationMs` in the RPC response — so a corrupt/unreadable asset still degrades gracefully to the old behavior instead of crashing. `Tests/windower-sidecar-macosTests/AudioAssetWriterTests.swift` and `VideoAssetWriterTests.swift` updated for the new `finish` signature.

**Verified:**
- `swift build` — clean.
- `swift test` — 122/122 pass (118 baseline + 4 new unit tests for `VideoAssetWriter.milliseconds(from:)` covering a numeric `CMTime`, `.indefinite`, `.invalid`, and `.zero`).
- Full MCP/daemon/sidecar path: killed the stale daemon (`apps/daemon/dist/bin.js`) so the MCP server would respawn one against the freshly built sidecar, called `mcp__windower__start_recording` against the `Built-in Retina Display` display target, waited ~6s, then `mcp__windower__stop_recording`. Manifest reported `video.durationMs: 11875`. `ffprobe` (`node_modules/.pnpm/ffprobe-static@3.1.0/.../ffprobe`) on the finalized mp4 (`Built-in Retina Display-2026-08-09T15-52-21.mp4`) reported `duration=11.875000` — an exact match to the millisecond, confirming `manifest.video.durationMs` is now asset-derived (same source `ffprobe` reads) rather than wall-clock. (This normal, no-frame-drop recording doesn't reproduce the original ~17.2s-vs-~7.8s divergence, but the exact match to `ffprobe` — rather than a wall-clock number that would only coincidentally agree to 3 decimal places — demonstrates the fix is reading the real asset duration.)

**Status:** Fixed.

## 6. Region/display capture can freeze on stale pixels for long stretches despite real on-screen activity

**Found:** Phase 18 live test, recording a `region` target matching a Chrome browser window while driving real scroll interactions via browser automation (`mcp__claude-in-chrome__computer` scroll actions) during the recording.

**Symptom:** First attempt: performed 3 distinct scroll actions with 1s waits between them during a ~26s recording, confirmed via live screenshots (accurate, DOM/CDP-based) that the page visibly progressed through 3+ different sections throughout. But the resulting video, when sampled frame-by-frame after the fact, showed the page sitting essentially motionless on one section for ~26 of the ~26s, with all real content changes compressed into the last ~1-2s. The recorded pixels did not track the actual on-screen/DOM state in real time — bumping the wait time between actions to 2-3s (three later attempts) fixed it, suggesting the screen-capture pipeline (or the window compositor) needs longer to "catch up" after an interaction than the interaction itself takes.

**Impact:** Recordings of fast-changing/interactive content can silently capture long stretches of stale/frozen frames even though the underlying app state changed correctly and promptly — the video is not a faithful record of what happened on screen, timing-wise. This is a serious correctness concern for the whole product ("Windower captures the session"), not just a Phase 18 inconvenience: any real demo with quick back-to-back interactions (not artificially spaced out by 2-3s waits) risks looking static in the output even though the actions happened.

**Suspected source:** Not investigated — could be `SCStream` frame delivery/backpressure in `CaptureService.swift` (Phase 4), GPU-compositor throttling of an occluded/backgrounded window that ScreenCaptureKit is reading from, or something specific to capturing a window/region that isn't the frontmost-and-focused surface at every instant. Notably: this was observed even when the target window *was* frontmost/focused per `list_targets`' `isFocused` field at capture start.

Worked around empirically by spacing interactions 2-3s apart during recording. Needs real investigation into `SCStream` frame-delivery timing under Phase 13's e2e/soak-test umbrella — this is exactly the kind of bug a 10+ minute soak test or a frame-timestamp-vs-wall-clock assertion would have caught.

**Diagnostics added (this session):** A follow-up pass found that `native/macos/Sources/WindowerSidecarCore/VideoAssetWriter.swift`'s `append(sampleBuffer:)` silently dropped any frame when `AVAssetWriterInput.isReadyForMoreMediaData` was `false` (or `input.append` itself returned `false`), with zero counting/logging — `CaptureService.swift`'s `CaptureStreamOutput.stream(_:didOutputSampleBuffer:of:)` discarded the return value entirely. There was no way to distinguish "SCK/WindowServer delivered stale/delayed frames" (upstream, outside this codebase) from "our own writer silently ate frames it was handed" (a real, in-our-control gap) — this is the actionable half of the bug, since region/display capture uses a full-display `SCContentFilter` with `sourceRect`/`destinationRect` cropping rather than a window-restricted filter, so `SCStreamConfiguration` tuning alone can't confirm or rule out the root cause. Added: (1) a `droppedFrameCount` counter on `VideoAssetWriter` and `AudioWriterInputHandle`, incremented on every `false` return from `append`; (2) PTS-gap ("stall") detection — `VideoAssetWriter` tracks the presentation timestamp of the last successfully-appended frame and, on each accepted frame, computes the gap since it; a gap exceeding `max(3x the configured frame interval, 500ms)` (`VideoAssetWriter.stallThresholdMs(forFps:)`) increments `stallEventCount` and updates `maxStallGapMs`; (3) `stopCapture` in `CaptureService.swift` now emits a `warn`-level `log` notification (existing notification type, unchanged wire shape — see `LogNotificationParams`) plus a direct stderr line summarizing both counters whenever either is nonzero, via `VideoAssetWriter.diagnosticsSummary()`. `StopCaptureResult`, the manifest schema, and `contracts/sidecar-protocol.md` are all unchanged — this is diagnostic-only instrumentation for the *next* time this reproduces, not a reported field.

**This does NOT confirm or rule out a root cause.** `swift build`/`swift test` (129/129, 122 baseline + 7 new unit tests covering `gapMilliseconds`/`stallThresholdMs`/stall-recording against synthetic `CMSampleBuffer`s) pass, and a real ~6s recording via the daemon/MCP path (killed the stale daemon first, confirmed `windower-sidecar-macos` respawned from the freshly built `native/macos/.build/debug` binary) completed cleanly with no crash and — as expected for a short, non-stressed recording — no drops/stalls, so no warn log fired. No attempt was made this session to force a genuine stall to see the new logging actually trigger.

**Live repro attempt (follow-up session):** Before repro'ing, traced where the new diagnostics actually surface end-to-end and found a real, separate gap: `apps/daemon/src/session-manager.ts` calls `this.spawnSidecar({ onExit, ... })` without an `onStderrLine` callback, and never subscribes to the sidecar's `"log"` notification (only `"event"` and `"captureEnded"` — grep confirmed no `.on("log"` anywhere in `apps/daemon/src`). `packages/core/src/process/sidecar-process.ts` does capture the sidecar's stderr line-by-line, but silently drops each line when `onStderrLine` is undefined. So as wired today, the bug #6 diagnostics (stderr line + `log` notification) are invisible through the normal CLI/MCP/daemon path — not fixed here since it's out of scope for this pass, but worth its own follow-up (wire `onStderrLine` to `console.error` and add a `client.on("log", ...)` handler in `session-manager.ts`, mirroring bug #2's fix).

Given that gap, repro'd by driving the compiled sidecar binary (`native/macos/.build/debug/windower-sidecar-macos`) directly over raw newline-delimited JSON-RPC on stdio (`describe` → `startCapture` → `stopCapture`), with stderr captured straight to a log file — bypassing the daemon so nothing could swallow the diagnostics. Four attempts:

1. **Region target, rapid CDP-driven scrolls (browser-extension automation), no explicit waits.** Video showed content completely unrelated to the page being scrolled — a YouTube video, then an unrelated coding-agent session, then a GitHub Actions page. Root cause: this machine's Chrome is a *single shared window* used concurrently by multiple other automated sessions on the same desktop (confirmed via `osascript` — one Chrome window, tabs including YouTube/GitHub Actions/ChatGPT/calendar alongside the target tab), and browser-extension automation drives tabs via CDP without necessarily bringing them to the real OS foreground. A `region` capture reads raw display compositor pixels, so it faithfully recorded whatever window/tab actually was frontmost — which wasn't the one being scripted. Not usable evidence either way for bug #6.
2. **Same region target, Chrome brought to real foreground via `osascript ... activate` first.** Better, but the video still showed unrelated content partway through (this machine also auto-switches active app during normal tool use, including this session's own Bash calls) — same shared-desktop contamination. Also not usable evidence for the region case.
3. **Window-kind target (`{kind:"window", id:"10979"}`) against a dedicated Chrome window created solely for this test**, driven with real OS-level keystrokes (`osascript`/System Events `key code 121`, not CDP) so the interaction is indistinguishable from a human at the keyboard. First pass fired 15 page-down/up/down presses back-to-back (<50ms apart) — video correctly showed the windower-site content (not contaminated, since window-kind capture reads the window's own surface via SCK regardless of on-screen occlusion/frontmost state) but all the motion landed in a single ~0.3s window, too fast to distinguish "correctly tracked" from "coarse sampling."
4. **Same window-kind setup, 10 page-down presses paced ~530ms apart** (faster cadence than the original bug's 1s-and-stalled case), with `eventTimeline` keystroke capture giving exact ground-truth timestamps (`t=4499,5043,5572,6096,6625,7163,7662,8153,8659,9201`ms into the session). Extracting frames every 0.5s and correlating against those timestamps: the video's visible scroll position advanced in lockstep, landing on each new section within ~0.5s (one sampling interval) of the corresponding real keystroke, reaching the bottom of the page by ~t=7.5–8s versus the last relevant keypress at ~t=7.7–8.2s. No multi-second lag, no content "compressed into the last 1–2s" pattern. `droppedFrameCount`/`stallEventCount` were 0 in every one of these four attempts (stderr empty, no warn `log` fired every time), including the two contaminated region attempts where the physical screen *did* undergo large, real, rapid content swaps.

**Conclusion:** Did not reproduce the original stale-pixels stall this session. For `window`-kind capture specifically, real rapid keyboard-driven interaction (faster cadence than the original failing case) tracked wall-clock state correctly with the new instrumentation reporting zero drops/stalls throughout — no evidence of either writer-side backpressure or SCK-side staleness under this test. The `region`-kind case (the one actually used in the original bug report) was not cleanly retested — this machine's desktop is shared by multiple concurrent automated sessions fighting over window focus, which makes `region` (raw display-compositor pixels) fundamentally unreliable to test here since any other session's window can occlude the target between frames; that occlusion-sensitivity is itself consistent with (but not proof of) the original hypothesis that region/display capture is more exposed to compositor-level effects than window capture. Next attempt should either use `window`-kind exclusively (given it appears immune to the desktop contamination and still exposes the same `VideoAssetWriter` pipeline), or be run on a non-shared machine/session so a `region` capture can be trusted to reflect only the intended interaction.

**Status:** Partially addressed, was not reproduced as of that session — **now reproduced with hard evidence, see update below.** Separately, a real (if minor) gap was found and left unfixed at the time: the daemon does not forward sidecar stderr or subscribe to `log` notifications, so as shipped these diagnostics are invisible through the normal CLI/MCP path — tracked as bug #7 below (fixed there; `packages/engine/src/recording-engine.ts` now wires both `onStderrLine` and `.on("log"` — see #7 for the fix, which the reproduction below confirms is wired but did not actually catch this occurrence, see "diagnostic blind spot" below).

**Reproduced — Phase 20 live-verification follow-up (new session).** Real `windower operate` run, `kind: "display"` (whole primary display, not window/region — a capture kind not covered by the prior session's repro attempts), against `anthropic:claude-sonnet-5`, task: open Safari via Finder → Applications, navigate to waroom.co. Session ran a real 201s wall-clock (`startedAt` 23:34:55.693Z → `stoppedAt` 23:38:16.680Z per `windower status`), and `.events.json` confirms 72 real synthetic input events spanning the full `t=4491ms` to `t=195836ms` of that window — clicks, scrolls, double-clicks, all landing (Safari genuinely opened; the operator's own screenshots show it open with a real new-tab page). But `manifest.json`'s `video.durationMs` — asset-derived per bug #5's fix, i.e. read from the actually-decodable `AVAsset`, not wall-clock — is **8991.67ms**, confirmed independently via `ffprobe`. So of a real 201-second interactive session, only the **first ~9 seconds** made it into playable video; the remaining ~192 seconds of genuine on-screen activity (opening Finder, navigating to Applications, launching Safari, clicking the address bar) never landed in the file, with **zero errors surfaced anywhere** — the RPC succeeded, the manifest looked normal, `stopCapture` reported no failure.

**Diagnostic blind spot found:** bug #6's own dropped-frame/stall instrumentation (`droppedFrameCount`, PTS-gap `stallEventCount` in `VideoAssetWriter.swift`) reported **zero** drops and zero stalls for this recording (`grep`'d the full stderr capture — no warn-level `log` line, and bug #7's fix confirms `RecordingEngine` *does* forward sidecar stderr/log for this daemon-free path, so absence of a warning is a genuine negative, not another instance of #7's forwarding gap). This makes sense on inspection: the stall detector only measures the *gap since the last successfully-appended frame* — it can only fire on a **subsequent** frame. If `SCStream` simply **stops delivering samples entirely** after some point and never resumes for the rest of the session, there is no "next frame" to compute a gap against, so the detector never trips, no matter how long the drought lasts. The instrumentation catches "frames are arriving late/intermittently" but not "frames stopped arriving at all" — the exact shape of this reproduction (a clean ~9s prefix, then nothing, for ~192s, right up to a normal-looking `stopCapture`).

**Impact:** This is a severe, now-confirmed instance of #6, worse than previously suspected — not occasional stale/frozen pixels during fast interaction, but a capture stream that can apparently stop delivering frames entirely partway through a long-ish session and never recover, with the daemon/engine having no way to detect or surface it. Any sufficiently long real recording (this one was ~3.5 minutes) is at risk of silently losing the majority of its content while reporting total success. This directly undermines Windower's core promise ("Windower captures the session") for exactly the kind of longer, real task `operate` exists to support.

**Suggested fix (in addition to #6's original suggestion):** add a *liveness* check independent of the existing gap-since-last-frame detector — e.g. a timer that fires if no frame has been appended within some threshold (say 2-3x the frame interval) *measured from wall-clock*, not from the last frame's PTS, so a total stop (not just a slow one) is still caught and logged. Also worth checking whether `SCStream`'s delegate/`didStopWithError` callback fired silently around the ~9s mark in this run (would need a repro with the sidecar's own raw stderr captured directly, bypassing the daemon/engine, as prior `bugs.spec.md` #6 sessions did) — if the stream itself reported an error that just wasn't surfaced through `stopCapture`'s result, that's a more direct root cause and an easier fix than chasing `SCStream` frame-delivery timing blind.

**Status:** Confirmed reproduced with concrete evidence (this session). Root cause (why frame delivery stops) still not isolated — needs a raw-sidecar repro with `SCStream` delegate error/state logging, per the suggested fix above. This is now the single most important open item in this file — silently-truncated recordings that report success are a correctness problem, not a diagnostics gap.

**Follow-up session — delegate audit, wall-clock liveness watchdog, and three more raw-sidecar repro attempts.**

**Delegate audit (step 1 of this session's investigation):** Read `CaptureService.swift` end-to-end. `SCStreamDelegate` IS correctly wired: `CaptureStreamDelegate` is created per-session, passed into `SCStream(filter:configuration:delegate:)`, and held via a strong reference from `CaptureSessionContext` for the session's full lifetime (not just at creation) — so it is not an unregistered-delegate or premature-deallocation bug. Its one method, `stream(_:didStopWithError:)`, forwards unconditionally to `CaptureSessionManager.handleStreamStoppedUnexpectedly`, which removes the session, stops mic/event-tap, cancels the writer, and emits a `captureEnded` notification — nothing here swallows or filters by error type. Conclusion: if `didStopWithError` had fired during the original ~9s-then-silence run, the session would have been torn down and a `captureEnded` notification would exist, AND the subsequent `stopCapture` call (which the original run made successfully ~192s later) would have failed with `SESSION_NOT_FOUND` instead of returning a normal result. Since the original run's `stopCapture` succeeded normally, `didStopWithError` almost certainly did **not** fire — `SCStream` went silent without erroring, which is the harder case (nothing in the delegate path was there to catch or mishandle).

**Root-cause hypothesis formed from code reading (step 1, continued):** `packages/engine/src/operator-run-engine.ts`'s `runOperator` (~line 240) resolves `client` via `this.sessionManager.getSidecarClient(sessionId)` — i.e. the operator's `captureFrame` calls (one per observe→decide→act loop iteration, `packages/operator/src/run.ts` line ~219) go over the **same sidecar process and same single-threaded stdio RPC loop** (`main.swift`'s `while let line = readLine()`) as the active recording's `SCStream`. `FrameCaptureService.captureImage` (`FrameCapture.swift` line ~266) bridges `SCScreenshotManager.captureImage`'s async API into that synchronous loop via `Task { ... semaphore.signal() }; semaphore.wait()` — the same blocking-bridge pattern used by `EnumerationService.fetchShareableContent` and `VideoAssetWriter.finish`. Blocking a thread on a semaphore while waiting on a `Task` scheduled onto Swift's cooperative concurrency thread pool is a known anti-pattern that can starve/deadlock that pool under load; if `SCStream`'s own internal frame-delivery plumbing also depends on that pool, repeated `captureFrame` calls interleaved with a live recording (exactly the operator's real usage pattern, and unique to `captureFrame` among the sidecar's blocking-bridge call sites — it's the only one invoked repeatedly *during* an active `SCStream` session) was a plausible mechanism for silently wedging frame delivery without ever raising a catchable error.

**Repro attempts (step 2), raw JSON-RPC over stdio directly against `.build/debug/windower-sidecar-macos`, stderr piped straight to a file, bypassing daemon/engine entirely:**
1. `display`-kind target, 200s wall-clock, real keystrokes into a dedicated TextEdit window every 4s via `osascript`/System Events (no `captureFrame` at all — baseline). Result: `stopCapture` returned `actualDurationMs: 200381.67`; `ffprobe` on the file confirmed `duration=200.381667` — exact match, no truncation.
2. Same `display` target, 200s wall-clock, but with a `captureFrame` RPC call every ~6-8s throughout (30 calls total) — directly testing the hypothesis above by mimicking the operator's real observe-loop cadence against the SAME sidecar process as the active recording. Result: all 30 `captureFrame` calls succeeded; `stopCapture` returned `actualDurationMs: 200201.67`, `ffprobe` confirmed `duration=200.381667`≈ match (200.201s) — no truncation, no drops, no stalls.
3. Same setup as #2 but with real application-launch activity matching the ORIGINAL failing scenario more closely: `osascript` drove Finder → open Applications folder → activate Safari (a genuinely new app launching mid-session, which is what was happening around the failure's ~9s mark in the original report), plus 26 `captureFrame` calls over a 160s session. Result: `stopCapture` returned `actualDurationMs: 159910`; `ffprobe` confirmed `duration=159.910000` — exact match, no truncation, zero drops/stalls/liveness failures logged.

None of the three attempts reproduced the original freeze. This is consistent with every prior session's experience with bug #6 — genuinely hard to force on demand, and the leading `captureFrame`-concurrency hypothesis, while structurally plausible from the code (the one blocking-bridge call site actually exercised repeatedly during a live `SCStream` session in production), is **not confirmed** by this session's direct testing of it. Root cause remains open.

**Fix applied (step 4, the safety-net explicitly requested regardless of root-cause status):** Since the existing PTS-gap stall detector structurally cannot catch a total stop (no subsequent frame to compute a gap against — see the "Diagnostic blind spot" note above), added a wall-clock liveness watchdog, fully independent of frame PTS:
- `native/macos/Sources/WindowerSidecarCore/VideoAssetWriter.swift`: new `checkLiveness(now:)` compares real wall-clock time (via an injectable `clock: () -> Date`, defaulting to `Date.init`, added to the initializer for testability) against `lastAcceptedAt` (updated on every accepted frame, seeded to the session-start time in `start(at:)` so a check running before the first frame still has a baseline) — NOT against any frame's PTS. Returns `true` only on the transition into a stalled state (so a caller logs once, not every poll). New `livenessThresholdMs(forFps:)` mirrors `stallThresholdMs(forFps:)`'s "3x frame interval" shape but floors at 2000ms instead of 500ms (it's checked on an external poll, not per-frame, so it needs headroom above the poll interval). New `hasLivenessFailure`/`maxLivenessGapMs` properties feed into the existing `hasDiagnosticWarnings`/`diagnosticsSummary()` (no wire-schema change, same pattern as the original bug #6 diagnostics).
- `native/macos/Sources/WindowerSidecarCore/CaptureService.swift`: `startCapture` now creates a repeating `DispatchSourceTimer` (polling at 1/3 of `livenessThresholdMs`, so a real stall is caught within one threshold window) that calls `writer.checkLiveness()`; on the first trip, emits a `level: "error"` `log` notification (existing notification type, `LogNotificationParams.level` already allows `"error"` per `packages/core/src/protocol/methods.ts`) in real time, not just at `stopCapture`. `CaptureSessionContext` gained a `livenessTimer` field (a `DispatchSource` needs a strong reference to keep firing, same caveat as every other per-session resource here) and the timer is explicitly canceled on every teardown path: `stopCapture`, `cancelCapture`, and `handleStreamStoppedUnexpectedly`.

**Build/test results:** `swift build` — clean. `swift test` — **191/191 pass** (185 baseline + 6 new: `testLivenessThresholdMsIsThreeFrameIntervalsAboveTheHigherFloor`, `testLivenessThresholdMsFallsBackToFloorForNonPositiveFps`, `testCheckLivenessReturnsFalseBeforeStart`, `testCheckLivenessStaysHealthyWhenFramesKeepArrivingWithinThreshold`, `testCheckLivenessTripsWhenNoFrameArrivesForLongerThanThreshold`, `testCheckLivenessOnlyReportsTheTransitionIntoFailureOnce` — all using a controllable injected `FakeClock` rather than a real sleep, same "synthetic delay" style as the existing PTS-gap stall tests).

**Confirmation repro (step 6):** No `ANTHROPIC_API_KEY` (or any other provider key) was available in this session's environment — the real `windower operate` end-to-end repro against a real model (the scenario that originally surfaced this bug) could not be run. Per this task's own fallback instructions, the three raw-sidecar repro attempts in step 2 above (200s, 200s, and 160s real captures with real interaction and, in two of the three, `captureFrame` calls at operator-loop cadence) stand in as the confirmation evidence instead. All three completed with `stopCapture`'s asset-derived `actualDurationMs` matching real wall-clock elapsed time and `ffprobe`'s independently-read `duration` to within tens of milliseconds — i.e. no truncation occurred in any of them, and the new liveness watchdog logged nothing (correctly — nothing stalled). This confirms the watchdog doesn't false-positive under sustained real captures with mixed `captureFrame`/interaction load, but does **not** confirm it catches the original freeze, since the freeze itself did not reproduce this session.

**Status:** Partially addressed. Delegate path audited and ruled out as the failure point (see above). Wall-clock liveness watchdog implemented, tested (191/191 swift tests pass), and confirmed not to false-positive across ~520s of combined real raw-sidecar capture this session — this closes the "silently loses the majority of a recording with zero diagnostic signal" half of the bug even if the underlying `SCStream` freeze cause is never found, since the next real occurrence will now log a `level: "error"` notification in real time instead of nothing. The `captureFrame`-concurrency hypothesis (the operator's per-step screenshot calls sharing one blocking, single-threaded RPC loop with an active `SCStream` recording) was investigated and specifically targeted by two of the three repro attempts, but did **not** reproduce the failure — so it is a documented, code-grounded lead for the next session, not a confirmed root cause. Root cause of the underlying freeze is still open. Next session, if the freeze recurs in real use with the new watchdog deployed, its `log` notification (and which of the three sub-cases logged — drops/PTS-stalls/liveness-only) will itself be the most direct evidence yet toward isolating the mechanism.

**Follow-up session — new lead ("record works, operate doesn't"), root cause isolated, mitigating fix shipped, and a fresh live reproduction confirms it.**

**New lead supplied at the start of this session:** plain `windower record` (no operator involved) does **not** exhibit the truncation — only `windower operate` triggers it. This narrows the search from "something about `SCStream`/`VideoAssetWriter` in general" to "something specific to the operate loop's RPC traffic interleaved with an in-progress capture," since `record` never issues any RPC besides `startCapture`/`stopCapture` against the sidecar, while `operate` additionally issues `performInput`, `enumerateTargets` (`list_targets`), and `captureFrame` once per step, all over the SAME sidecar process/connection as the active `SCStream`.

**Code audit (this session):**
- `native/macos/Sources/windower-sidecar-macos/main.swift`'s dispatch loop (`while let line = readLine(strippingNewline: true) { handle(line:) }`) is the sidecar's *only* thread servicing JSON-RPC — there is no `RunLoop.run()` anywhere on it, and no concurrency: `handleRequest` runs every method (`enumerateTargets`, `performInput`, `captureFrame`, `startCapture`, ...) synchronously, one at a time, on this one thread.
- `EventTapCapture.swift` already gets this right for the one thing that actually needs a live `CFRunLoop` (`CGEventTap`): it spins up its own dedicated background thread with its own `CFRunLoopRun()` (see the class doc at `EventTapCapture.swift:157-164`) — ruled out as a contributor.
- `CaptureService.swift`'s `SCStream` sample-buffer delegate is registered on its own dedicated serial `DispatchQueue` (`windower.capture.<sessionId>`, `CaptureService.swift:634`) — not the main thread, not the RPC-dispatch thread. So `SCStream`'s frame delivery does not directly contend for the same OS thread as `readLine()`/`handleRequest`.
- `InputSynthesis.swift`'s `CGEventPost` path (`performInput`) does not block or require a run loop — ruled out.
- That left the two blocking-bridge call sites that actually run **on the RPC-dispatch thread itself** and are invoked repeatedly during a live `operate` run: `EnumerationService.fetchShareableContent()` (`Enumeration.swift:28`, used by both `enumerateTargets`/`list_targets` **and** by `captureFrame`'s target resolution) and `FrameCaptureService`'s `captureImage` (`FrameCapture.swift:257`, the `SCScreenshotManager.captureImage` semaphore bridge). Both used an **unconditional** `semaphore.wait()` with no timeout — if the completion handler/`Task` they're waiting on never fires, the sidecar's one and only RPC-dispatch thread blocks **forever**, meaning no method of any kind (including a subsequent `stopCapture`) can ever be serviced again. This is a structural hazard independent of *why* the completion handler might not fire.

**Live reproduction, this session, real `windower operate` (no daemon, real `ANTHROPIC_API_KEY`, model `anthropic:claude-sonnet-5`, task: open Finder → Applications → Safari → navigate to waroom.co):** The exact same failure signature as the prior session's live stall reproduced again, with the swift-side liveness watchdog (shipped last session) now instrumented and firing in real time:

```
[operate] step 1 @ 4.2s: click({"x":75,"y":792}) -> {"performed":1}
[operate] step 2 @ 7.9s: screenshot({}) -> {"ok":true,...}
[operate] step 3 @ 11.8s: list_targets({}) -> [...]
[RecordingEngine] sidecar stderr: capture liveness check failed ... no frame appended for over 2000ms
[RecordingEngine] sidecar stderr: capture diagnostics ...: liveness check failed: no frame appended for 8096ms
```

Immediately after `list_targets` returned at t=11.8s, the operator's next step called `captureFrame` (screenshot), which internally calls `EnumerationService.fetchShareableContent()` for target resolution — and, this time with this session's timeout fix already in place (see below), that call's `SCShareableContent.getWithCompletionHandler` **did not complete within the 8000ms bound**, and the sidecar reported it as a clean `CAPTURE_FAILED` error ("captureFrame failed: SCShareableContent did not respond within 8000ms") instead of hanging forever. The operator run then failed cleanly with `OPERATOR_DEPENDENCY_ERROR`, the session was torn down, and the whole `windower operate` process **exited on its own** ~20s after starting — no manual `kill -9` needed, unlike the prior session's occurrence of this same freeze (which required killing both the CLI and sidecar processes after over a minute of total silence). `ps aux` afterward showed no orphaned sidecar/daemon process.

The recording itself still truncated in exactly the originally-reported shape, just over a shorter window this time: `manifest.json`'s asset-derived `video.durationMs` is **11925ms**, against a real session wall-clock span of `stoppedAt (00:35:55.305Z) − startedAt (00:35:35.141Z) = 20164ms` (`~/.windower/sessions/15fcedc1-c3cf-4b01-b043-060b4c23b16d.json`) — i.e. the last ~8.2s of the session (matching the watchdog's reported 8096ms gap almost exactly) produced no usable video, consistent with `SCStream` frame delivery having genuinely stopped at the same moment `fetchShareableContent` got stuck.

**Root cause (now well-supported, not just hypothesized):** `SCShareableContent.getWithCompletionHandler`'s completion handler can fail to fire (indefinitely, based on the prior session's minutes-long silent hang) when called on a process that also has an active `SCStream` recording — and this codebase calls it repeatedly during exactly that condition, once per `operate` step, via both `enumerateTargets` and `captureFrame`. Because that call runs synchronously on the sidecar's single RPC-dispatch thread with an unbounded wait, a hung completion handler doesn't just fail that one RPC — it wedges the *entire* sidecar, explaining both halves of the originally-reported symptom (no more frames get appended, matching `SCStream`'s own delivery apparently stopping at the same time, and no more RPCs get serviced at all, matching the user's observation that even the real on-screen interaction stopped making progress). This is consistent with `record`'s immunity: a plain recording never calls `fetchShareableContent`/`captureImage` while a stream is live, so it never exercises the hang at all.

**Fix applied (scoped to `native/macos`, no protocol change):**
- `Enumeration.swift`: `EnumerationService.fetchShareableContent(timeoutMs:)` now bounds its `semaphore.wait()` to 8000ms (default, overridable) via `semaphore.wait(timeout:)` instead of an unconditional `wait()`; on timeout it throws a new `EnumerationError.timedOut(afterMs:)` case rather than hanging.
- `main.swift`'s `enumerateTargets` handler and `FrameCapture.swift`'s `fetchShareableContent()` wrapper both now catch `EnumerationError.timedOut` explicitly and map it to the existing `CAPTURE_FAILED` taxonomy code (no new wire-protocol error code needed) with a message naming the timeout.
- `FrameCapture.swift`'s `captureImage` (the `SCScreenshotManager.captureImage` semaphore bridge) gets the identical treatment: a new `FrameCaptureService.captureImageTimeoutMs` (10000ms default) bounds its `semaphore.wait()`, timing out to a `CAPTURE_FAILED` error instead of hanging.
- Deliberately **not** applied to `CaptureService.swift`'s two remaining semaphore-bridges (`awaitCompletion` wrapping `stream.stopCapture`, and `VideoAssetWriter.finish`) — those are invoked once per session at `stopCapture`/`cancelCapture`, not repeatedly during a live capture, and the original bug's own reproductions show `stopCapture` completing normally even after the freeze; bounding them isn't motivated by any observed symptom and risks truncating a legitimately slow `finishWriting` on a long/high-bitrate recording. If a `stopCapture` hang is ever observed, revisit then with its own evidence-driven timeout value.
- This does **not** fix why `SCShareableContent`'s completion handler stops firing in the first place (still an open ScreenCaptureKit-level mystery, possibly OS-version-specific or resource-contention-dependent) — it fixes the far more severe compounding failure mode: a single stuck system completion handler taking down the sidecar's entire RPC loop, and with it the whole `operate` run, permanently and silently.

**Build/test results:** `swift build` clean, `swift test` **191/191 pass** (no regressions, no new tests added — the timeout logic is a thin wrapper around stdlib `DispatchSemaphore.wait(timeout:)` with no new pure logic to unit-test independent of a real hung `SCShareableContent` call, which cannot be forced deterministically in a unit test).

**Confirmation repro:** The live `windower operate` run described above **is** the confirmation — it hit the real freeze condition live (not synthetically) and demonstrates the fix working exactly as designed: an 8-second bounded wait instead of an indefinite hang, a clean `OPERATOR_DEPENDENCY_ERROR` surfaced to the caller, and a fully self-terminating process with no orphaned sidecar left behind. This is a stronger confirmation than a clean no-freeze run would have been, since it caught the actual bug in the act.

**What is still NOT fixed:** the underlying video is still truncated when this happens — the fix converts "silent infinite hang, recording quietly loses everything after some point, nothing ever recovers" into "bounded ~8-10s stall, clean error, recording preserves everything up to the stall point, caller can retry" — a major reliability improvement, but the root truncation (losing the tail of the recording once `SCShareableContent`/`SCScreenshotManager` wedges) is not eliminated, because `SCStream`'s own frame delivery genuinely does stop when this happens (not just the RPC that queries it). A complete fix would need to either (a) find why `SCShareableContent`/`SCScreenshotManager` contend with a live `SCStream` in the first place and avoid the contention (e.g. moving `enumerateTargets`/`captureFrame` off the main dispatch thread onto their own dedicated queue so they can time out without ever having blocked the one thread `SCStream`-adjacent bookkeeping might also need — not yet attempted, would touch more of `main.swift`'s architecture), or (b) have the daemon/engine layer detect the liveness-watchdog `log` notification in real time and automatically retry/restart the capture rather than just reporting failure after the fact (a `packages/engine` change, out of scope for this pass).

**Status:** Root cause narrowed and well-evidenced (SCShareableContent's completion handler can hang indefinitely on a process with an active SCStream, and `operate`'s per-step `enumerateTargets`/`captureFrame` calls are what exercise this — `record` never does, explaining the "record works, operate doesn't" split). A scoped, tested mitigating fix shipped (bounded timeouts on both semaphore-bridge call sites that run on the sidecar's single RPC-dispatch thread during a live capture), and a live, real `windower operate` run this session reproduced the actual freeze condition and confirmed the fix converts it from an indefinite silent hang into a clean, bounded, reported error — a major reliability win even though the underlying `SCShareableContent`/`SCStream` contention itself, and the resulting video truncation up to the stall point, remain open. Next session: either instrument `SCShareableContent`/`SCScreenshotManager` calls with OS-level tracing (e.g. `log stream` / Instruments) during a forced-repro window to find what they're actually contending on, or move these calls off the shared RPC-dispatch thread entirely so a stall there can never threaten the dispatch loop itself even before the timeout fires.

**Follow-up session — structural fix (RPC dispatch off the single thread), thread-safety audit, and a real repro that overturns the leading root-cause theory.**

**Verified baseline before starting:** `swift build`/`swift test` still 191/191 green, matching the prior session's claim.

**Thread-safety audit (before changing anything, since the prior session's own "still open" note explicitly proposed moving `fetchShareableContent`/`captureImage` off the RPC-dispatch thread and asked whether that's safe):**
- `CaptureSessionManager.shared`'s session dictionary (`CaptureService.swift:405-406`) is already `NSLock`-protected, and by its own doc comment (`:398-401`) this is *because* SCStream delegate callbacks already arrive on arbitrary background queues concurrently with the RPC thread today — so dispatching RPC handling concurrently adds no new class of hazard to session lookup/insert/remove (`startCapture`/`removeSession`, used by `stopCapture`/`cancelCapture`/`handleStreamStoppedUnexpectedly`, all go through the lock).
- `VideoAssetWriter`'s diagnostic counters (`droppedFrameCount`, `stallEventCount`, `maxStallGapMs`, `lastAcceptedAt`, `hasLivenessFailure`, `maxLivenessGapMs`, `hasStarted`) were genuinely **not** lock-guarded — a real, pre-existing (not introduced by this session) race between `append`/`recordAcceptedFrame` (running on the session's `sampleQueue`, SCStream's delegate queue) and `checkLiveness` (running on the session's independent `livenessQueue` timer). Since this session's change lets `stopCapture`'s `diagnosticsSummary()`/`finish()` call run on a background RPC queue too (a third uncoordinated accessor instead of two), fixed this properly rather than leaving it: `VideoAssetWriter.swift` now guards every one of those fields behind a single `NSLock` (`diagnosticsLock`), with `diagnosticsSummary()` snapshotting all counters under one lock acquisition for internal consistency. No wire/schema change, no test changes needed (existing 6 liveness/stall tests still pass unmodified since the public API shape is unchanged, just now lock-guarded internally).
- Checked `packages/core/src/protocol/sidecar-client.ts`'s `SidecarClient` (the TS-side caller of this protocol) before touching `main.swift`, per this task's own instruction to check whether the protocol already assumes possible concurrent/out-of-order responses: `call()` (`sidecar-client.ts:163-188`) assigns a fresh numeric `id` per request and stores `{method, resolve, reject}` in `private readonly pending = new Map<JsonRpcId, PendingCall>()`; `handleResponse` (`:240-261`) looks up and resolves purely by `id`, with **zero assumption that responses arrive in send order**. So making the sidecar answer out of order (or concurrently) is protocol-conformant today, not a break of an implicit assumption — confirmed by reading the actual client code, not just inferring from the spec prose. Added one clarifying paragraph to `contracts/sidecar-protocol.md`'s Transport section making this explicit for future Windows/Linux backends (no schema change, doc-only).

**Structural fix applied (`native/macos/Sources/windower-sidecar-macos/main.swift`):** the `while let line = readLine(strippingNewline: true)` loop no longer calls `handleRequest` inline. It still synchronously reads and JSON-frames each line (cheap, never blocks on anything but stdin), but for a `request` line it now does `inFlightRequests.enter(); rpcQueue.async { handleRequest(...); inFlightRequests.leave() }`, where `rpcQueue` is a concurrent `DispatchQueue(qos: .userInitiated)`. This means a `captureFrame`/`enumerateTargets` call stalled inside its (already-bounded, from the prior session) `semaphore.wait(timeout:)` no longer prevents `readLine()` from reading, framing, and immediately dispatching the *next* line — e.g. a `stopCapture` for the same or a different session — to run concurrently on another `rpcQueue` thread, rather than queuing up behind the stalled call for up to its 8-10s timeout. `writeLine`'s existing `stdoutLock` already serializes concurrent writers (previously needed only for SCStream's `captureEnded` notification vs. the main loop; now also needed for concurrent `rpcQueue` responses, same mechanism, no change needed). `inFlightRequests` (a `DispatchGroup`) is `wait()`-ed on after the `readLine` loop exits (stdin EOF) so the process can't fall off the end of `main.swift` while a dispatched request's response hasn't been written yet.

**Build/test results:** `swift build` clean, `swift test` **191/191 pass**, no regressions, no new tests added (same reasoning as the prior session's timeout change — the new behavior is scheduling/concurrency, not new pure logic; the existing `VideoAssetWriter` diagnostic tests already exercise the locked code paths and still pass, confirming the lock refactor didn't change observable behavior).

**Live repro — real `windower operate`, no daemon, real `ANTHROPIC_API_KEY`, model `anthropic:claude-sonnet-5`, identical task to the prior session's (Finder → Applications → Safari → waroom.co), `--max-steps 30`:** Ran to genuine completion this time — `state: "succeeded"`, all 30 steps executed, **zero errors, zero `CAPTURE_FAILED`/`OPERATOR_DEPENDENCY_ERROR` anywhere in the full stderr capture**, and critically, no multi-second stalls before any RPC response (every step's tool call returned promptly at normal per-step latency — contrast with the prior session, where the run died ~20s in). This confirms the structural fix's own goal: the RPC-dispatch thread is no longer blockable by a stalled `enumerateTargets`/`captureFrame` call, and (in this run) neither call ever needed its timeout at all — both completed quickly on the background queue with no observable disruption to `performInput`/other RPCs.

**But the video is still catastrophically truncated — worse in absolute and proportional terms than every prior confirmed occurrence:**
- Session wall clock (`~/.windower/sessions/78fea3ca-830f-44e7-a4ff-8ca6ccfbfef8.json`): `startedAt` 00:46:17.346Z → `stoppedAt` 00:49:38.615Z = **201,269ms**.
- `manifest.json`'s asset-derived `video.durationMs`: **5,416.67ms** — independently re-verified by loading the actual `.mp4` via `AVURLAsset.load(.duration)` in a standalone Swift snippet (bypassing the app's own manifest-writing code entirely): `duration_seconds=5.416666666666667`, an exact match.
- `.events.json` has 93 real synthetic input events (clicks/scrolls/keys) spanning `t=10982ms` to `t=189821ms` — genuine interaction across essentially the whole session, confirming Safari really did open and get driven the whole time, same as every prior confirmed occurrence of this bug.
- **Only ~2.7% of the session's real duration made it into the video** (5.4s of 201.3s), versus the immediately-prior session's confirmed occurrence of ~59% (11,925ms of 20,164ms — though that run was cut short at ~20s by the since-then-fixed RPC hang, so it never got the chance to lose more). In absolute terms: ~195.9s lost this run vs. ~8.2s lost previously.
- The wall-clock liveness watchdog fired exactly once, right after the very first RPC call of the run — `enumerateTargets`/`list_targets` at `t=5.3s` (operator step 1) — and **never cleared**: `stopCapture`'s final diagnostics line reports `"liveness check failed: no frame appended for 195064ms"`, i.e. the gap was still open and growing at the moment the session ended, ~195 seconds after it first tripped. `droppedFrameCount`/`stallEventCount` were both 0 throughout (no "frame(s) dropped" or "stall event(s)" text in the diagnostics summary) — consistent with every prior occurrence: this is not backpressure or intermittent slowness, it is `SCStream` frame delivery stopping **outright and permanently** right after the first `enumerateTargets` call, never resuming for the rest of the session.

**This overturns the leading hypothesis from every prior session.** The RPC-thread-blocking theory (a hung `SCShareableContent`/`SCScreenshotManager` completion handler wedging the sidecar's one thread, which this session's fix specifically targets) predicts that *removing* the thread-blocking should, at minimum, not make truncation worse, and ideally should let frame delivery continue in parallel with a slow/stuck call since they no longer share a thread. Instead: this run's `enumerateTargets` call completed **quickly and successfully** (no timeout, no error, confirmed by the total absence of `CAPTURE_FAILED`/timeout text anywhere in stderr) — and `SCStream` frame delivery still died at that exact moment anyway, for the rest of the session. The prior sessions' own repro numbers are consistent with this on re-reading: the "Reproduced — Phase 20" entry (9s of 201s video, zero drops/stalls, no RPC hang at all — this was *before* any timeout fix existed) shows the identical shape, and the "record works, operate doesn't" session's live reproduction also died immediately after its first `list_targets` call. Across (at least) three separate sessions now, the trigger is consistently "the first `enumerateTargets`/`captureFrame` call made to a sidecar with a live `SCStream`," not "a call that happens to hang." **The actual mechanism is not (or not only) a hung completion handler — invoking `SCShareableContent.getWithCompletionHandler` (and/or `SCScreenshotManager.captureImage`) at all while a `SCStream` is active appears able to permanently break that stream's own frame-delivery pipeline, independent of whether the call itself is fast, slow, or times out.** This means no RPC-thread-side mitigation — not the prior session's bounded timeouts, not this session's off-thread dispatch — can fix the truncation, because the damage (to `SCStream`, not to the RPC layer) happens regardless of which thread issues the call or how long it takes.

**What this session's fix DID accomplish, confirmed by direct evidence, and what it explicitly did NOT:**
- **Fixed and confirmed:** the sidecar's RPC-dispatch thread can no longer be blocked by a stalled `enumerateTargets`/`captureFrame` call — `stopCapture`/other RPCs remain serviceable throughout (this run needed no such rescue since nothing stalled, but the mechanism enabling it — concurrent dispatch off a single thread — is now in place and unit/build-verified; the last session's live freeze, which *did* require exactly this rescue, will no longer be able to wedge the whole process even in the cases where the underlying `SCShareableContent` call genuinely hangs rather than just breaking the stream).
- **NOT fixed, and per this session's evidence, structurally *cannot* be fixed by any thread/timeout-based mitigation:** the video still catastrophically truncates, immediately upon the first `enumerateTargets`/`captureFrame` call during a live recording, in a way this session's repro shows is the same or worse than ever previously measured. A `windower operate` recording today should be assumed to capture only the first few seconds of video, reliably, regardless of how long the real task actually runs — this is a severe, still-completely-open correctness problem, not a diagnostics gap and not something the existing mitigations reduce the frequency or severity of.
- **A new, important side effect worth flagging explicitly:** because this run produced no error of any kind (`state: "succeeded"`, clean manifest, clean transcript), this failure mode is now **more** silent than the immediately-prior session's, not less — a user watching only exit codes/state fields has zero signal anything went wrong. The only surviving signal is the `log`-notification/stderr diagnostic line from the liveness watchdog, which (per bug #7) is now correctly forwarded through `RecordingEngine` but is not surfaced anywhere in the CLI's own terminal output or `operate`'s JSON result today.

**Root cause: still not conclusively isolated, but now much more narrowly characterized than "a hang."** No OS-level tracing (`log stream`/Instruments) was attempted this session either — this remains the single biggest gap across every session on this bug. Two candidate mechanisms, neither confirmed:
1. A resource/connection-level conflict: `SCShareableContent`'s enumeration and `SCStream`'s frame delivery may share a connection to `replayd`/`WindowServer` that a control-plane query (`getWithCompletionHandler`) can disrupt or reset for the data-plane stream, even when the query itself succeeds quickly.
2. Some kind of internal SCStream reconfiguration or resource reclamation triggered by *any* `SCShareableContent`/`SCScreenshotManager` call against the same process while streaming — possibly OS-version-specific — that silently and permanently stops the delegate's `stream(_:didOutputSampleBuffer:of:)` callback from firing again, without ever invoking `didStopWithError` (independently reconfirmed this session: `stopCapture` succeeded normally at the end, which is only possible if the stream object was never torn down by the delegate path).

No Apple documentation, release note, or WWDC reference was found or searched for this session (out of time budget) — still an open item for whoever picks this up next.

**Suggested next steps, in order of expected value:**
1. OS-level tracing (`log stream --predicate '...' --level debug` or Instruments' ScreenCaptureKit/replayd-adjacent instrument) during a live repro, specifically bracketing the exact moment of the first `enumerateTargets`/`captureFrame` call, to see what actually happens to the `SCStream`/`replayd` connection at that instant. This has been recommended by at least two prior sessions and still has not been attempted.
2. Investigate whether `enumerateTargets`'s window/display listing can be served from a **different** API (e.g. `CGWindowListCopyWindowInfo` for windows, `CGGetActiveDisplayList`/`CGDisplayBounds` for displays) that doesn't touch whatever `SCShareableContent` touches, reserving `SCShareableContent`/`SCContentFilter` construction for the one-time `startCapture` setup and `captureFrame`'s per-shot filter only — evaluate whether the resulting `CaptureTarget` fields (title, bundle id, focus state, precise pixel bounds) can still be populated without SCK's own window enumeration.
3. If (1)/(2) don't pan out: consider isolating `enumerateTargets`/`captureFrame` calls made *during* an active recording into a **separate process** from the one running the live `SCStream`, so a per-process resource conflict (if that's what this is) becomes physically impossible rather than merely rare. This is a materially bigger architecture change (a second sidecar-like process, or routing observation calls through a distinct short-lived helper) and should not be attempted without first trying to confirm the mechanism via (1).
4. Independent of root cause: surface the liveness-watchdog `log` notification through `operate`'s own CLI/JSON output (today it only reaches stderr/the `log` notification per bug #7's fix, not the terminal `OperatorRun` result or exit code) so a truncated-but-"succeeded" run is at least visible to a human without grepping stderr — a `packages/engine`/`packages/cli` change, small and valuable regardless of whether the underlying SCStream issue is ever fixed.

**Status:** The RPC-dispatch-thread half of this bug (the sidecar being unable to service other requests, or even respond to its own timeout-bounded calls in a timely fashion, while `fetchShareableContent`/`captureImage` was stalled) is now structurally fixed and build/test-verified — `main.swift`'s dispatch loop can no longer be blocked by any single request's handler, confirmed protocol-conformant against the actual TS `SidecarClient` implementation (not just the spec prose), and `VideoAssetWriter`'s diagnostic state gained a real lock closing a pre-existing (unrelated) race this change would otherwise have made three-way instead of two-way. **The video-truncation half of this bug is emphatically NOT fixed, is not reduced in likelihood or severity by anything shipped in this or the immediately-prior session, and this session's live repro produced the single worst measured occurrence to date (2.7% of session duration preserved, zero errors surfaced, a "successful" `operate` run whose video is almost entirely missing).** Do not close this bug or describe it as mitigated based on the absence of `OPERATOR_DEPENDENCY_ERROR`/timeouts — this session is direct proof that a clean, error-free `operate` run can still lose ~97% of its video. The root cause is now better characterized (triggered by invoking `SCShareableContent`/`SCScreenshotManager` at all during a live `SCStream`, not specifically by that call hanging) but not fixed; OS-level tracing during a live repro remains the top recommended next step and has not been attempted in any session to date.

**Follow-up session — OS-level tracing finally attempted, root mechanism identified with a timestamped smoking-gun log excerpt, fix implemented in `packages/engine`+`packages/cli` (not `native/macos`), and quantitatively confirmed.**

**Verified baseline before starting:** `swift build`/`swift test` still 191/191 green (this session made no Swift changes at all — the fix landed entirely in TypeScript).

**Methodology:** Built a raw-sidecar JSON-RPC-over-stdio repro harness (Python, driving `.build/debug/windower-sidecar-macos` directly) to try to isolate the trigger cheaply, bracketing every attempt with `/usr/bin/log stream --style compact --predicate '(process == "windower-sidecar-macos") OR (process == "replayd") OR (process == "WindowServer") OR (subsystem CONTAINS "ScreenCaptureKit") OR (subsystem CONTAINS "SkyLight") OR (subsystem CONTAINS "coremedia") OR (subsystem CONTAINS "avfoundation")'` redirected to a file (no sudo available in this environment — `log stream`'s default, non-`--debug` verbosity turned out to be sufficient; `--info --debug` was never needed). Tried, in order: a single `enumerateTargets` call mid-stream (after a 5s warm-up and at fps 2), a single `captureFrame` call mid-stream, both at fps 30/quality high matching real `operate` defaults, both issued **immediately** after `startCapture` returns (no warm-up, matching real `operate`'s actual near-zero gap between `startCapture` and the first `captureFrame`), and a multi-step sequence mimicking the operator's exact observe→act cadence (`captureFrame`→`click`→`captureFrame`→`click`→`captureFrame`→`list_targets`→...). **None of these reproduced any truncation** — every raw single-process repro produced an asset-verified duration matching wall clock almost exactly (e.g. 17.5s captured of a 17.5s session with a mid-stream `enumerateTargets` call at fps 30). This matches every prior session's experience and initially looked like another dead end.

**The breakthrough came from going back to the real thing instead of trying to shrink the repro further.** Per this task's fallback instructions, ran a real `windower operate` (no daemon, real `ANTHROPIC_API_KEY`, `anthropic:claude-sonnet-5`, identical Finder→Applications→Safari→waroom.co task, `--max-steps 30`) with the same `log stream` bracketing it live. It reproduced the freeze in 20 seconds, right after the model's step-3 `list_targets` call, letting the OS log be correlated precisely:

```
2026-08-09 22:11:14.336 Df windower-sidecar-macos[52257:53195d7] (ScreenCaptureKit)  [INFO] +[SCShareableContent getShareableContentWithCompletionHandler:]:46
2026-08-09 22:11:14.336 Df windower-sidecar-macos[52257:53195d7] [com.apple.xpc:connection] [0x135606800] activating connection: mach=true listener=false peer=false name=com.apple.replayd
...
2026-08-09 22:11:14.389 Df replayd[468:5313dc1] [com.apple.xpc:connection] [0x7c4140f00] invalidated because the client process (pid 52257) either cancelled the connection or exited
2026-08-09 22:11:14.393 Df replayd[468:5313dc1]  [INFO] -[RPClientProxy stream:didStopWithError:]:164
```

**The smoking gun: `pid 52257` is a *second, separate* `windower-sidecar-macos` process — not the one holding the active recording (`pid 51729`, confirmed distinct throughout the whole run via `grep -o "windower-sidecar-macos\[[0-9]*:"`).** It appears out of nowhere at 22:11:14.336 (13ms after the model's `list_targets` tool call landed), makes exactly one `SCShareableContent` call, and exits at 22:11:14.389 — and 4ms later, `replayd` logs `RPClientProxy stream:didStopWithError:` for the *other* process's stream. Root cause found in code, not just logs: `packages/engine/src/passthrough.ts`'s `PassthroughService.listTargets`/`resizeWindow` **spawn a brand-new sidecar process for every call and terminate it immediately after** (`const handle = this.spawnSidecar({}); ... await handle.client.enumerateTargets(...); ... await handle.terminate()`) — by design, for callers with no active recording session. But `packages/engine/src/operator-run-engine.ts`'s `createDeps` (and an independent, deliberately-duplicated copy in `packages/cli/src/commands/operate-blocking.ts`, the actual code path a plain `windower operate` invocation runs, since `operate` defaults to blocking/local mode per `contracts/cli.md`'s policy table) routed the operator's `list_targets`/`resize_window` tools through **exactly this passthrough**, even while a recording's own sidecar was actively streaming. Every real `operate` failure in every prior session's logs correlates with a `list_targets` (or, per the code, `resize_window`) call for precisely this reason: it's not that `SCShareableContent` breaks a live stream by being invoked — it's that spawning a **second OS process that briefly touches ScreenCaptureKit and then exits** causes `replayd` to invalidate the **first** process's stream (`RPClientProxy stream:didStopWithError:`), a resource-scope conflict at the `replayd`/WindowServer level, not a per-call API incompatibility. This also explains why every raw single-process repro (this session's and every prior session's) failed to reproduce it: none of them ever spawned a second sidecar process — they only ever issued `enumerateTargets`/`captureFrame` **within** the one process already holding the stream, which is safe (confirmed independently by all four of this session's own single-process repro attempts).

**Fix implemented (`packages/engine`/`packages/cli`, no `native/macos`/protocol change — the sidecar-protocol wire shape is untouched):**
- `packages/engine/src/operator-run-engine.ts`'s `createDeps`: `listTargets`/`resizeWindow` now call `ctx.client.enumerateTargets(...)`/`ctx.client.resizeWindow(...)` directly — the *same* already-running sidecar process backing the active recording (or the transient one for `--no-record`, which is likewise already `ctx.client`) — instead of `this.passthrough.listTargets/resizeWindow`, which would spawn a second process. `list_targets`'s `"app"` kind (accepted by the daemon-level schema but not the sidecar protocol, which only knows `"display"|"window"`) is filtered the same way `PassthroughService.listTargets` already did, so behavior for that case is unchanged.
- `packages/cli/src/commands/operate-blocking.ts`'s `createDeps` — a **second, independent implementation** of the identical logic (this file is `operate`'s local/blocking path per its own header comment: "does not share `operator-run-engine.ts`'s internals... the fix has to be applied in both places") — got the identical fix. This is the file that actually ran during every real confirmation repro in this and prior sessions (`operate` defaults to `local`/blocking mode, not `--detach`/daemon mode), and is why the first attempt at re-confirming the fix this session (before this file was found and patched) still reproduced the exact same failure — the `operator-run-engine.ts`-only fix had zero effect on a plain `windower operate` invocation.
- `resolveOperatorTarget()` in both files is **unchanged** — it still calls `passthrough.listTargets`/spawns a transient sidecar, but only *before* any recording starts, when there is no live `SCStream` yet to disrupt.

**Tests added:** `packages/engine/src/operator-run-engine.test.ts` — new test asserts exactly 2 sidecar spawns for a run that calls `deps.listTargets`/`deps.resizeWindow` mid-loop (1 for `resolveOperatorTarget`'s pre-stream call, 1 for the recording itself — critically NOT 3). `packages/cli/src/commands/operate-blocking.test.ts` — mirrors the same assertion (1 spawn expected there, since the recording sidecar in that test's fixture comes from a mocked `RecordingEngine`, not `spawnSidecar`). Both new tests fail against the pre-fix code and pass against the fix.

**Build/test results:** `pnpm turbo build --filter=@windower/cli...` clean. `pnpm turbo test` for `@windower/core` (166), `@windower/engine` (83, +1 new), `@windower/daemon` (19), `@windower/operator` (52), `@windower/cli` (183, +1 new) — all green, no regressions. `swift build`/`swift test` untouched, still 191/191 (this fix required no Swift changes).

**Confirmation repro, quantitative, against both known-bad baselines:** Real `windower operate` (no daemon, real `ANTHROPIC_API_KEY`, `anthropic:claude-sonnet-5`, identical Finder→Applications→Safari→waroom.co task, `--max-steps 30`), OS log tracing running throughout. Ran the full 30 steps to `OPERATOR_MAX_STEPS_EXCEEDED` (a guardrail outcome unrelated to this bug — the model didn't finish the task in 30 steps this run, immaterial to video-duration measurement) over **203,734ms** of real wall clock (`startedAt` 01:27:18.773Z → `endedAt` 01:30:42.570Z, cross-checked against the session file's own `startedAt`/`stoppedAt`). `manifest.json`'s asset-derived `video.durationMs`: **165,491.67ms**, independently re-verified via a standalone `AVURLAsset.load(.duration)` Swift snippet against the actual `.mp4` (bypassing manifest-writing code entirely): `duration_seconds=165.49166666666667` — exact match.

| Run | Wall clock | Video preserved | % preserved |
|---|---|---|---|
| Baseline 1 (pre-existing, "record works operate doesn't" session) | 20,164ms | 11,925ms | 59.5% |
| Baseline 2 (pre-existing, worst prior occurrence, "structural fix" session) | 201,269ms | 5,417ms | 2.7% |
| **This session, post-fix** | 203,734ms | 165,492ms | **81.2%** |

`grep -o "windower-sidecar-macos\[[0-9]*:" oslog.log \| sort -u` across the entire 203s run's OS log confirms **exactly 2 sidecar PIDs the whole time** (one transient pre-stream, one long-lived recording process) despite the model calling `list_targets` three separate times mid-run (steps 8, 12, 20) — the fix's core claim (no second ScreenCaptureKit process is ever spawned while a stream is live) holds for the entire real run, not just synthetically.

**What this does NOT fix — honest remaining gap:** 81.2% is a large, real improvement over both baselines but is **not** 100%. The sidecar's own diagnostics for this run: one liveness-watchdog trip early (`no frame appended for over 2000ms`, shortly after step 2's click, self-recovered — frames resumed since the final preserved duration is far more than a clean prefix would allow), and by `stopCapture` time, a cumulative **37 PTS-gap stall events (longest single gap 12,241ms)** plus the stream ending in a stalled state again (`liveness check failed: no frame appended for 42397ms`). This is a materially different, milder failure shape than the pre-fix pattern (one catastrophic, *permanent*, un-recovering death immediately after the first `list_targets`/`captureFrame` call) — it now looks like ordinary intermittent `SCStream`/encoder backpressure under sustained heavy real interaction (rapid clicks/double-clicks, a Spotlight search, typing), the same milder class of symptom this bug file originally opened with (`bugs.spec.md` #6's very first, pre-truncation-discovery entry: "stale pixels for long stretches"). Whether this residual ~19% loss is `VideoAssetWriter` backpressure, WindowServer compositor throttling, or something else was not investigated further this session — it did not block using this real run as strong quantitative confirmation of the fix, since it's a fundamentally different and far less severe symptom than what this bug has tracked as its main open item since being reproduced with hard evidence.

**Status: substantially fixed, no longer "emphatically NOT fixed."** The catastrophic, permanent, error-free truncation mechanism (a second ScreenCaptureKit-touching process spawned by `PassthroughService` for `list_targets`/`resize_window` during a live `operate` run, silently killing the recording's own `SCStream` via a `replayd`-level resource conflict) is identified with direct, timestamped OS-level log evidence (not just correlation) and fixed by eliminating the second process entirely — `list_targets`/`resize_window` now always reuse the live recording's own sidecar. Quantitatively confirmed: 81.2% of a real 203.7s `operate` session preserved, vs. 59.5% and 2.7% on the two prior confirmed-bad baselines — a large, real improvement, not just "better than the worst case." A milder, residual intermittent-stall issue (~19% additional loss, self-recovering, consistent with ordinary capture backpressure rather than a second-process conflict) remains open and is a much smaller, lower-priority problem than what this bug tracked before this session — worth a future pass (e.g. reviving `VideoAssetWriter`'s existing drop/stall counters to pinpoint whether it's writer-side or `SCStream`-side) but not the "silently loses 97-100% of the video while reporting success" class of failure this bug was about. Do not re-open this as "not fixed" without new evidence of the *permanent, error-free, second-process-triggered* death recurring — that specific, previously 100%-reproducible mechanism is what this session's fix targets and what its evidence supports as resolved.

**Follow-up session — investigated the residual ~19% (37 PTS-gap stalls) mechanism, blocked from a live-model repro by an invalid API key, extensive raw-sidecar stress-testing found no reproduction, shipped a real-time diagnostic improvement instead of a speculative behavioral fix.**

**Verified baseline before starting:** `swift build`/`swift test` 191/191 green. Re-read `packages/engine/src/passthrough.ts`, `operator-run-engine.ts`, `packages/cli/src/commands/operate-blocking.ts` — confirmed the prior session's fix (both `createDeps` implementations call `ctx.client.enumerateTargets`/`resizeWindow` directly, never `this.passthrough`) is intact and unmodified. No daemon/sidecar processes were left running from any prior session (`ps aux` clean at start).

**Blocker hit immediately:** this session's `.env` `ANTHROPIC_API_KEY` (well-formed, `sk-ant-api03-...`, 108 chars, no whitespace/encoding issue — checked byte-for-byte with `od -c`) is rejected by Anthropic's API with a plain `401 authentication_error: "API key is invalid."`, confirmed independently via a raw HTTPS request (bypassing the CLI/SDK entirely) to rule out a windower-side parsing bug rather than a genuinely revoked/expired credential. This is a new, environment-level blocker not present in any prior session on this bug — every previous session's live `windower operate` repro assumed a working key. No keychain or alternate credential source was accessible (sandboxed from `security find-generic-password` and broad filesystem search by the harness's own permission classifier). **A real, live-model `windower operate` repro was not possible this session** — this is an honest gap, not a finding.

**Fallback: raw-sidecar harness replicating operate's cadence as closely as possible without a model.** Per this task's own fallback instructions (used by multiple prior sessions when blocked), built a Node.js JSON-RPC-over-stdio harness (`/private/tmp/.../scratchpad/bug6/harness.mjs`, not committed — scratchpad only) driving the compiled `.build/debug/windower-sidecar-macos` directly: `startCapture` on the primary display (fps 30, h264, quality high) immediately followed by (1) a real Spotlight-driven Safari launch and waroom.co navigation via real `performInput` `key_press`/`type_text` RPCs — genuine app-launch system load, matching the original bug's own task — then (2) ~180s of continued periodic `captureFrame` (maxWidth 1280, matching `packages/operator/src/run.ts`'s `DEFAULT_FRAME_MAX_WIDTH`)/`enumerateTargets`/`performInput` calls at operator-like cadence, all against the SAME sidecar process throughout (matching the now-fixed architecture — no second process ever spawned). Ran three variants:
1. Baseline cadence (~2.5-5s per step, matching real per-step model latency): 183.24s wall clock, `stopCapture`'s `actualDurationMs` 183,240ms, independently `ffprobe`-verified (via `ffprobe-static`'s darwin/x64 binary, since no system `ffprobe` was installed) duration 183.240000s — exact match, zero drops/stalls/liveness failures.
2. Tighter cadence (500-1500ms per step) plus rapid double-click bursts (mirroring the confirmed run's own diagnostic note: "rapid clicks/double-clicks, a Spotlight search, typing" as the coincident load pattern) plus 2 background `yes > /dev/null` processes for sustained CPU load: 180.26s wall clock, `actualDurationMs` 180,231.67ms, `ffprobe` duration 180.231667s — exact match again, zero drops/stalls/liveness failures.
3. Same as (2) but with 12 background `yes` processes (saturating all 14 cores, load average driven from ~4 to ~17 during the run): 180.28s wall clock, `actualDurationMs` 180,231.67ms, `ffprobe` duration 180.231667s — **identical result to (2) down to the millisecond**, zero drops/stalls/liveness failures even under heavy CPU contention.

**None of the three attempts reproduced a single PTS-gap stall, dropped frame, or liveness failure**, despite deliberately targeting both candidate mechanisms named in this task's brief: (a) `captureFrame`'s `SCScreenshotManager` call repeatedly contending with the live `SCStream` in the same process (tested at up to ~2x the real operate cadence), and (b) plain encoder/writer backpressure under genuine system load (tested with real app-launch activity, rapid click bursts, and full CPU saturation). Also cross-checked the OS-level log (`log stream` bracketing all three runs, same predicate as the prior session's breakthrough) for the sidecar's own PID — no `invalidated`/`didStopWithError` entries for the recording process in any run, consistent with the sidecar's own diagnostics reporting a clean session throughout. **This is a genuine negative result, not a dead end from insufficient effort** — it extends the well-established pattern from every prior session on this bug (raw single-process repros never reproduce either the catastrophic or, now, the milder failure mode) to this specific milder mechanism as well. The milder stalls appear to require something only present in a real `windower operate` run with a real model driving it — possibly the actual TypeScript/Node operator process's own resource usage (image encoding/decoding for the model, network I/O to the provider) running concurrently with the daemon-free CLI process and the sidecar on the same machine, or genuinely varying real-world screen content complexity (a real page rendering, real animations) that a static desktop + cursor movement doesn't replicate. Next session with a working API key should re-attempt the exact real-`operate` repro from the prior two sessions' table, this time cross-referencing the new real-time stall logging below against `.events.json`.

**Diagnostic gap found and fixed (no behavioral change):** the existing PTS-gap stall detector (`VideoAssetWriter.stallEventCount`/`maxStallGapMs`) only ever surfaced as a rolled-up count in `diagnosticsSummary()` at `stopCapture` time — e.g. the confirmed run's "37 PTS-gap stall events (longest gap 12,241ms)" was known only in aggregate, with **no way to determine when during the 200s session any individual stall occurred**, making it impossible to correlate against operator step timestamps without adding new instrumentation first (exactly what this session needed but a real run couldn't exercise). Fixed in `native/macos/Sources/WindowerSidecarCore/CaptureService.swift`: the existing liveness-watchdog `DispatchSourceTimer` (already polling at 1/3 of the liveness threshold, ~660ms-2s depending on fps) now also tracks `lastLoggedStallEventCount` and, on each tick, diffs it against `writer.stallEventCount`; any increase emits a real-time `warn`-level `log` notification (`"capture PTS-gap stall detected ... N new stall event(s) (cumulative M, longest gap so far Xms)"`) via the existing `emitLog` helper — same notification type and wire shape as the liveness watchdog's own `error`-level log, no protocol/schema change. This means the *next* real occurrence of these stalls will produce a timestamped log line per stall (within one poll interval of when it actually happened, not just a final count at session end), which is the missing piece for correlating against `.events.json`/the operator's step log per this task's step 2. Deliberately did **not** touch `VideoAssetWriter.append`'s drop-on-backpressure behavior (the "step 4" hypothesis in this task's brief): Apple's own guidance for live real-time capture is to drop a frame immediately when `isReadyForMoreMediaData` is false, not block waiting for it — blocking on the `SCStream` sample-buffer delegate queue (where `append` runs) risks reintroducing exactly the class of hazard already fixed in an earlier session (a stalled call on a queue `SCStream`-adjacent bookkeeping may depend on). Changing that behavior without evidence it's actually the mechanism — which this session's stress testing did not find — would be exactly the kind of speculative fix this bug's history has repeatedly warned against.

**Build/test results:** `swift build` clean, `swift test` **191/191 pass**, no regressions. No new unit tests added for the `CaptureService.swift` change — same reasoning as two prior sessions' scheduling-only changes (the timeout fix, the RPC-dispatch-off-single-thread fix): this is timer/logging wiring around an existing, already-tested pure-logic accessor (`writer.stallEventCount`), not new pure logic itself, and exercising it meaningfully requires an actual live `SCStream` session that unit tests don't set up.

**Confirmation repro:** Not possible this session (see blocker above). The three raw-sidecar stress runs above stand in as evidence that the new logging doesn't false-positive (all three logged zero stall lines, correctly, since none occurred) and that `swift build`/`swift test` aren't regressed — but do **not** confirm the new real-time stall logging actually fires correctly on a genuine stall, since no genuine stall was produced. That confirmation is deferred to the next session with a working model credential.

**Status: unchanged from the prior session's assessment of the residual issue — still open, still believed to be a minor/self-recovering issue relative to the (now-fixed) catastrophic mechanism, not newly characterized.** This session did not find the mechanism, did not change any capture/writer behavior, and does not claim to have made progress narrowing the root cause — only (a) ran the most thorough negative-result stress-test to date (real app launch + rapid clicks + full CPU saturation, still zero reproduction) further reinforcing that this is not a trivially-forced synthetic condition, and (b) shipped real-time per-stall logging so the next live repro doesn't have to re-derive this instrumentation from scratch. The main bug (permanent, catastrophic, error-free truncation via the second-process mechanism) remains fixed and should not be re-opened. The residual ~19%/37-stall issue remains open, low-priority relative to the main bug, and now has better tooling in place for whoever next has a working API key to pick it up.

## 7. Daemon never surfaces the sidecar's `log` notifications or stderr — bug #6's diagnostics are invisible through the normal CLI/MCP path

**Found:** Same live-repro follow-up session as bug #6, while tracing where bug #6's new dropped-frame/stall diagnostics actually surface.

**Symptom:** `apps/daemon/src/session-manager.ts` spawns the sidecar via `this.spawnSidecar({ onExit, ... })` without an `onStderrLine` callback, and never subscribes to the sidecar's `"log"` notification anywhere (grep confirmed no `.on("log"` in `apps/daemon/src` — only `.on("event"` and `.on("captureEnded"` are wired). `packages/core/src/process/sidecar-process.ts` does capture the sidecar's stderr line-by-line, but silently discards each line whenever no `onStderrLine` callback was supplied.

**Impact:** Any `log` notification the sidecar emits (bug #6's new `warn`-level drop/stall summary, and the pre-existing keyboard-capture-unavailable warning from Phase 10's `EventTapCapture`/`main.swift`) is a dead letter through the normal `windower` CLI / MCP / Claude Code plugin path — none of it reaches a daemon log, a CLI `--json` output, or an MCP tool result. The only way to see this diagnostic output today is to bypass the daemon entirely and drive the compiled sidecar binary directly over raw JSON-RPC with stderr piped to a file by hand (as done to work around this exact gap during bug #6's live-repro attempt). This silently defeats the whole point of adding the bug #6 instrumentation for any real caller.

**Suspected fix:** mirror bug #2's fix pattern (`apps/daemon/src/session-manager.ts` already logs swallowed narration/event-timeline errors via `console.error`) — pass an `onStderrLine` callback into `spawnSidecar` that forwards each line to `console.error` (or whatever the daemon's established logging convention is), and add a `client.on("log", (params) => console.error(...))` handler alongside the existing `.on("event"`/`.on("captureEnded"` subscriptions in `session-manager.ts`, so `log` notifications are at minimum visible in the daemon's own process output. Not fixed yet.

**Fix:** `apps/daemon/src/session-manager.ts`'s `startRecording` (~line 204) now passes an `onStderrLine` callback into `spawnSidecar` that forwards each sidecar stderr line to `console.error` (`[SessionManager] sidecar[<id>] stderr: <line>`), and registers a `handle.client.on("log", ...)` handler alongside the existing `.on("event"` subscription (~line 241) that logs each `log` notification via `console.error` (`[SessionManager] sidecar[<id>] log (<level>): <message>`). Both bug #6's diagnostics (dropped-frame/stall warnings) and the pre-existing keyboard-capture-unavailable warning are now visible in the daemon's own process output through the normal CLI/MCP/daemon path — no daemon RPC/manifest shape changed.

**Verified:** `tsc --noEmit` (`apps/daemon`) — clean. `vitest run` (`apps/daemon`) — 62/62 pass, no regressions.

**Status:** Fixed.

## 8. `ensureDaemonRunning`-spawned fixture daemons leak forever across test runs

**Found:** Phase 20 live-verification pre-check (this session). `ps aux | grep windower` on a dev machine that has run the test suite showed ~80+ orphaned `fake-daemon-cli.mjs` processes, dating back hours across many separate `pnpm test`/`vitest` invocations, none reaped.

**Root cause:** `packages/core/src/daemon/connect.test.ts` has several tests (`"ensureDaemonRunning spawns a fresh daemon..."`, `"auto-restarts once, safely, when..."`, `"treats a hello-less (pre-Phase-20) daemon..."`, and others) that call `ensureDaemonRunning(...)`, which internally calls `spawnDaemonDetached()` — a real `child_process.spawn(..., { detached: true }).unref()`. These tests only call `client.dispose()` in cleanup, which closes the socket connection, not the spawned process. Unlike the tests that spawn the fixture directly via the local `spawnFixture()` helper (pushed onto a `spawned: ChildProcess[]` array and `SIGKILL`ed in `afterEach`), daemons spawned indirectly through `ensureDaemonRunning` never get a handle captured anywhere, so nothing can kill them. The real production daemon self-terminates via `checkIdle`'s 30-minute timer; the fake fixture (`packages/core/src/daemon/fixtures/fake-daemon-cli.mjs`) implements no such timer, so these orphans live forever until the machine reboots or someone manually `pkill`s them. `packages/cli/src/commands/doctor.test.ts` uses the same fixture and is a second, smaller source.

**Impact:** Harmless individually (idle Node processes holding an unused unix socket), but compounds without bound — every `pnpm test` run adds more, `ps aux` becomes unreadable for anyone trying to eyeball real windower processes (exactly what Phase 20's live-verification checklist needs to do cleanly), and each one holds a stale test-tmpdir `daemon.json`/socket open. Not a production bug — `apps/daemon`'s real binary was never observed to leak — purely a test-fixture cleanup gap.

**Suggested fix:** `spawnDaemonDetached` (or a test-only wrapper) should return the spawned pid/`ChildProcess` so `connect.test.ts` can push it onto the same `spawned` array the direct-spawn tests already use and kill it in `afterEach`, mirroring the existing pattern. Alternatively, give `fake-daemon-cli.mjs` its own short idle-shutdown timer (e.g. 5s) so orphans self-reap even if a test-cleanup path is missed.

**Workaround used this session:** `pkill -f fake-daemon-cli.mjs` before starting Phase 20 live verification, so `ps aux | grep windower` reads meaningfully.

**Status:** Fixed. Added optional `onSpawn?: (child: ChildProcess) => void` to `EnsureDaemonRunningOptions` (`packages/core/src/daemon/connect.ts`), invoked in `spawnAndConnect` right after `spawnDaemonDetached`; `connect.test.ts` now passes `onSpawn: (child) => spawned.push(child)` on every `ensureDaemonRunning`/`restartDaemon` call that can spawn, so the fixture gets `SIGKILL`ed in the existing `afterEach`. No production call site sets `onSpawn`, so real-caller behavior is unchanged. `doctor.test.ts` checked — `doctor.ts` never calls `ensureDaemonRunning`/`spawnDaemonDetached`, its one spawn was already pushed onto `spawned`; no change needed there.

## 9. Operator run crashes with `OPERATOR_MODEL_ERROR` when the model emits a duplicate `toolCallId` in one turn

**Found:** Phase 20 live-verification follow-up (this session), a real `windower operate` run driving Safari (open via Finder → Applications, navigate to waroom.co) against `anthropic:claude-sonnet-5`.

**Symptom:** After 28 successful steps (~3.4 minutes) the run terminated with `state: "failed"` and:
```
{ "code": "OPERATOR_MODEL_ERROR", "message": "messages.56.content.1: each tool_use must have a single result. Found multiple `tool_result` blocks with id: toolu_016LbcUe18cWzw2apszsmGC8" }
```
This is Anthropic's own API rejecting the *next* request — the operator loop had already built and sent a malformed message on the previous turn.

**Root cause:** `packages/operator/src/run.ts`'s per-step loop (~line 285) iterates `modelResult.toolCalls` and, for every call, unconditionally pushes an entry onto `toolResults` keyed by `call.toolCallId` (~line 322, and the `UNKNOWN_TOOL` branch at ~line 290). All entries then get flattened into a single `tool`-role message's content array (~lines 341-350), one `tool-result` block per entry, with **no dedup on `toolCallId`**. If the model's response for a single turn ever contains two tool calls sharing the same `toolCallId` — observed here: step 26 called `press_key({key:"Escape"})` and `click({x:639,y:61})` in the same turn (both succeeded normally) — and the underlying id collides (either a genuine duplicate id from the provider/AI SDK, or a stale id echoed back from an early single-tool-per-turn convention the loop doesn't enforce), the resulting message ends up with two `tool-result` blocks carrying the identical id, which the Anthropic Messages API hard-rejects on the very next call. Immediately preceding this collision, step 27 (`toolCalls[27]`) had already produced an `UNKNOWN_TOOL` result for a misnamed `key_press` call (the model's actual tool is named `press_key`, per `contracts/operator.md`) — a separate, minor, self-correctable model mistake, not itself the crash cause, but consistent with the run being in a slightly confused state right before the fatal turn.

**Impact:** Any real operator run is vulnerable to a hard, unrecoverable crash (not a retryable/guardrail-bounded failure — it's a raw provider-API rejection surfacing as `OPERATOR_MODEL_ERROR`) whenever the model's tool-call batch for a single turn contains a duplicate `toolCallId`. Since this reproduced organically on a real, moderately long run (28 steps) with no adversarial input, it is plausibly not rare — longer/more complex operator tasks are more turns, more exposure. The recording itself still finalized correctly (this bug is scoped to the operator loop, not the recording engine — Phase 20's daemon/recording-lifecycle guarantees held), so impact is "the operator task doesn't complete," not data loss.

**Suggested fix:** in the `toolResults`-building loop in `run.ts`, dedupe by `call.toolCallId` before pushing (keep first or last, either avoids the collision — first is probably closer to "what actually happened") and log or record when a collision occurs, since a silent dedupe is easier to defend than a silent duplicate. Worth also checking whether this is an AI SDK v7 quirk with the Anthropic provider under multi-tool-calls-per-turn specifically (this operator setup intentionally allows the model to call more than one tool per turn — see `packages/operator`'s v7 usage note elsewhere in this doc) — if so, a note in `packages/operator`'s file-header comment about the SDK version would help the next person who hits this.

**Status:** Fixed — `run.ts`'s tool-call loop now tracks `seenToolCallIds` and skips (with a log line) any subsequent call whose `toolCallId` repeats within the same turn, so only the first occurrence is executed and only one `tool-result` block per id is ever emitted.

**N/A as of Phase 24 — the Operator no longer exists.**

## 10. `windower operate --detach` fails with `OPERATOR_MISSING_API_KEY` even though the daemon's environment has the key

**Found:** Phase 21 live verification (this session), on the very first caller-driven repro attempt. The bug had never been hit because Phase 20's live verification only exercised *blocking* `windower operate` (local, in-process, never sends `hello`), never the `--detach` daemon-backed path.

**Symptom:** `windower operate --detach` fails within ~2s with `state: "failed"` and 0 steps:
```
{"code":"OPERATOR_MISSING_API_KEY","message":"Missing API key for provider \"anthropic\": set ANTHROPIC_API_KEY in the daemon's environment..."}
```
— even though the calling shell had `ANTHROPIC_API_KEY` exported *and* the daemon process itself had it in its environment (verified live with `ps eww` on the daemon pid).

**Root cause (chain, all verified from source):** `packages/cli/src/backend.ts:72` called `ensureDaemonRunning()` with no options, so `buildHelloParams` (`packages/core/src/daemon/connect.ts:200-209`) sent `env: undefined` in `hello`. `packages/engine/src/request-context.ts:47-59` then produced `env: {}` — an empty object, not `undefined`. `packages/engine/src/operator-run-engine.ts:283` did `context ? { ...context.env } : undefined`, and `{}` is truthy, so the empty object was passed as the operator loop child's `env`. `packages/operator/src/run.ts:208` calls `resolveModel(options.model, options.env)`, so `providers.ts:65`'s `env = process.env` default never applied and the key was never found. The loop child process *did* inherit the full daemon environment (`packages/engine/src/operator-loop-host.ts:121`), so the correct value was present and deliberately ignored.

**Why it escaped tests:** `packages/engine/src/operator-run-engine.test.ts` hand-constructed a `RequestContext` already containing the key and passed it straight in; there was no test for `buildRequestContext` at all (no `request-context.test.ts` existed); and `packages/cli/src/commands/operate.test.ts`'s `--detach` test asserted only that `{ runId }` came back, never anything about `hello.env`. Both halves of the contract were tested in isolation and the seam between them was untested — the same class of miss as the earlier commander `--json` parse-layer bug recorded in `STATUS.md`: asserting on a builder in isolation cannot catch a wiring bug.

**Not a Phase 21 regression:** `git show HEAD:packages/engine/src/operator-run-engine.ts` has the identical `context ? {...} : undefined` line, `packages/cli/src/backend.ts` is unchanged vs `HEAD`, and `request-context.ts` landed with these semantics in Phase 20 (commit `80017c6`). This is pre-existing Phase 20 behavior, surfaced by Phase 21's verification because Phase 21's caller-orchestrated recipe (`start_recording` → `run_operator` → poll → `stop_recording`) uses the detached path.

**Contract was already correct:** `specs/001-windower-mvp/contracts/daemon-rpc.md:52` already says "Only `operate --detach` and any other daemon-backed flow that needs a secret goes through this." The CLI simply did not honor it. No contract change was needed.

**Fix applied:** `buildOperatorHelloEnv` lifted from `packages/mcp-server/src/operator-env.ts` into `packages/core/src/daemon/operator-env.ts` so CLI and MCP share one implementation (mcp-server's module is now a one-line re-export); `packages/cli/src/backend.ts` gained an optional `env` on `WithBackendOptions` and now calls `ensureDaemonRunning({ clientName: "windower-cli", env })`; `packages/cli/src/commands/operate.ts`'s `--detach` branch builds that env from `params.model`/`params.secrets`. Defensively, `packages/engine/src/operator-run-engine.ts:283` now treats an empty snapshot as "nothing scoped" (`context && Object.keys(context.env).length > 0 ? ... : undefined`) so it falls back to `process.env` rather than being shadowed by `{}`.

**Tests added:** new `packages/engine/src/request-context.test.ts` (6 tests); 1 test in `operator-run-engine.test.ts` (empty context env must not shadow `process.env`); 3 tests in `packages/cli/src/commands/operate.test.ts` driven through the real command registration/parse, asserting the `hello` actually carries the resolved API-key var name and value. Suites after the fix: core 218, engine 161 (was 154), daemon 22, operator 101, cli 218 (was 215), mcp-server 53; `pnpm turbo run test` 19/19.

**Status:** Fixed and verified live — after the fix, a real caller-driven `start_recording` → `operate --detach` → poll → `stop_recording` run reached the operator loop and executed 29 real steps against `anthropic:claude-sonnet-5`.

**N/A as of Phase 24 — the Operator no longer exists.**

## 6 (continued) — Follow-up session: API key fixed, live repro of the residual ~19%-class stall obtained, correlated against operator steps, no code fix applied (evidence argues against every candidate fix)

**Context:** the immediately-prior session was blocked from a live-model repro by an invalid `ANTHROPIC_API_KEY` and fell back to a synthetic raw-sidecar harness (three stress variants, zero stalls forced) plus shipped real-time per-stall `log`-notification/stderr logging (`CaptureService.swift`'s liveness-watchdog timer now diffs `writer.stallEventCount` on every tick) so the *next* live occurrence would be timestamped instead of only summarized at `stopCapture`. This session started with a working key (confirmed via a direct HTTPS call to `api.anthropic.com/v1/messages` with `model: "claude-sonnet-5"`, bypassing the CLI/SDK — 200 OK) and picked up exactly where that session left off.

**Verified baseline before starting:** `swift build`/`swift test` **191/191 green**, no regressions. Confirmed both fixes from prior sessions are intact and unmodified: `packages/engine/src/operator-run-engine.ts` and `packages/cli/src/commands/operate-blocking.ts` both still call `ctx.client.enumerateTargets`/`resizeWindow` directly (never `this.passthrough`) for in-loop `list_targets`/`resize_window`; `native/macos/Sources/WindowerSidecarCore/CaptureService.swift` still has the real-time per-stall `log` line (`lastLoggedStallEventCount` diffed against `writer.stallEventCount` on every liveness-timer tick). `ps aux` was clean (no orphaned sidecar/daemon/`log stream` processes) before starting.

**Live repro, run 1 (the confirmed data point this entry is based on):** No daemon, real `windower operate` (`packages/cli/dist/index.js operate`, blocking/local mode — `operate` does not take `--kind`; it always records the primary display and re-targets itself via its own tools, a CLI usage detail this session had to discover by trial), `anthropic:claude-sonnet-5`, `--max-steps 30`, identical task to every prior session's (Finder → Applications → Safari → waroom.co → screenshot), OS-level `log stream` bracketing the run (same predicate as the session that found the second-process root cause). The run reached `state: "succeeded"` in 23 steps.

- Session wall clock (`~/.windower/sessions/f3ef2477-3a9c-4a44-aea6-0aaf0be5f9d3.json`): `startedAt` 2026-08-10T14:42:48.465Z → `stoppedAt` 2026-08-10T14:45:40.837Z = **172,372ms**.
- `manifest.json`'s asset-derived `video.durationMs`: **174,151.67ms** — independently re-verified via `ffprobe` (`ffprobe-static`'s darwin/arm64 binary) directly on the `.mp4`: `duration=174.151667` — exact match.
- **~101% of the session preserved** (video duration is a hair longer than wall clock, not shorter — normal PTS/encoder rounding, not evidence of any problem). This is a materially better result than the prior fixed-baseline session's 81.2% (165,492ms / 203,734ms), though the two runs aren't perfectly comparable (different session length, different real-world model behavior/step count this time — the model finished the task in 23 steps here vs. hitting `OPERATOR_MAX_STEPS_EXCEEDED` at 30 steps in the 81.2% session).
- `.events.json`: 80 real synthetic input events spanning `t=4,554ms` to `t=160,758ms` — genuine interaction across essentially the whole session, Safari genuinely opened and was driven throughout (confirmed by watching the resulting video directly).
- Sidecar diagnostics at `stopCapture`: **13 PTS-gap stall events, longest gap 5,125ms**, plus one liveness-watchdog trip right at the very end (`no frame appended for 4,915ms` — fired as the session was already stopping, not a mid-session freeze). **Zero dropped frames** (`droppedFrameCount` — the diagnostics summary omits the drop clause entirely when it's zero, and it was omitted here), confirming this is not `VideoAssetWriter` rejecting frames on backpressure; the gaps are upstream, in `SCStream`'s own frame delivery.

**Correlation against operator step timestamps (the specific question this and the prior 2 sessions could not answer without a working key) — real-time stall log lines interleaved with `[operate] step N @ Xs: <tool>(...)` lines, in actual chronological order:**

| Step | t | Tool call(s) | Stalls immediately after |
|---|---|---|---|
| 0–3 | 4.5s–20.2s | click, list_targets ×2, click+wait | none |
| 4–7 | 68.2s–96.5s | click ×4 (Dock → Finder → Applications) | none |
| 8 | 100.3s | click | **1 stall** (1,117ms) |
| — | — | (between steps 9/10) | liveness trip (2,000ms) + **1 stall** (4,283ms) |
| 11–12 | 109.3s–114.6s | scroll, click | **3 stalls** (cumulative 3–5, 4,992ms each) |
| 13–14 | 118.9s–123.1s | click+type_text, click | **2 stalls** (cumulative 6–7) |
| 18 | 148.5s | double_click | **1 stall** (cumulative 8) |
| 20 | 157.5s | type_text ("waroom.co") | **3 stalls** (cumulative 9–11) |
| 22 | 164.8s | wait+screenshot | **2 stalls** (cumulative 12–13, 5,125ms) |
| 23 | 172.1s | done | final diagnostics line, session ends |

**Findings, directly from this table — answers the correlation question this task exists to resolve:**
1. **Zero stalls during steps 0–7** (t=4.5s–96.5s, ~92 seconds) — this is the Finder/Dock portion: simple, mostly-static desktop content, two `list_targets` calls (steps 1 and 3) included, neither followed by any stall.
2. **All 13 stalls cluster from step 8 onward** (t≈100s–172s) — exactly the portion once Safari is open and rendering a real, animated, network-loading web page (scrolling, typing into the address bar, page navigation via Return, the final screenshot).
3. **No consistent correlation with any specific RPC type.** The two `list_targets` calls (steps 1, 3) have zero adjacent stalls. `captureFrame`/screenshot calls are inconsistent: step 16's `wait+screenshot` (t=143.7s) has no adjacent stall, but step 22's `wait+screenshot` (t=164.8s) is immediately followed by 2 stalls. Clicks, scrolls, type_text, and double_click all appear both with and without adjacent stalls. This rules out the two leading candidate mechanisms named in this task's brief as the *sole* driver: it is not "stalls happen right after `captureFrame`" and not "stalls happen right after `list_targets`."
4. **Zero dropped frames throughout** — the writer never rejected a frame on `isReadyForMoreMediaData == false`. Combined with (3), the evidence points to `SCStream` itself pausing frame delivery for a few seconds at a time during real, visually complex/animated content (a live web page loading/rendering), not a `VideoAssetWriter`-side or RPC-triggered mechanism. This matches the *original*, pre-truncation-discovery description of this bug (`bugs.spec.md`'s very first #6 entry: "region/display capture can freeze on stale pixels for long stretches despite real on-screen activity") more closely than either of the two RPC-based hypotheses from the truncation investigation.

**No code fix applied this session — the evidence argues against every candidate fix named in this task's brief, not just "none was tried":**
- The `captureFrame`/`SCScreenshotManager`-concurrency hypothesis (cache the latest live-stream frame instead of a separate `SCScreenshotManager` call) is not supported: stalls do not consistently follow `captureFrame` calls (see finding 3), and the catastrophic version of this hypothesis was already ruled out for the *permanent*-death mechanism in an earlier session (fixed by eliminating the second-process spawn, not by touching `captureFrame` itself).
- The `AVAssetWriterInput` backpressure/`requestMediaDataWhenReady`-queuing hypothesis is not supported: `droppedFrameCount` was 0 for the entire run, meaning the writer was never handed a frame it refused — there is no backpressure at the writer boundary to fix. The gap is entirely upstream of `append()`.
- Changing `VideoAssetWriter.append`'s drop-on-backpressure behavior anyway (queuing instead of dropping) — which this session did **not** do, per the immediately-prior session's own reasoning, still reaffirmed here — would risk reintroducing exactly the class of hazard already fixed two sessions ago (a stalled call on a queue `SCStream`-adjacent bookkeeping may depend on), for a mechanism this run's own diagnostics show isn't actually a writer-side drop problem.
- The most honest characterization: this is ordinary `SCStream`/WindowServer-compositor backpressure under real, dynamic on-screen content (a genuinely loading/animating web page), which Apple's own live-capture guidance treats as expected and recoverable (frames resume — confirmed by the ~101%-preserved total duration, i.e. no net content loss, just brief real-time staleness during the gap) rather than something the sidecar should "fix" by blocking or restructuring its capture pipeline. `swift build`/`swift test` unchanged, still **191/191**, no Swift code touched this session.

**Second confirmation run:** started (also showed stalls climbing quickly — 8 cumulative, longest gap 7,417ms, by ~78s in) but was cancelled by the user (via the coordinator; PIDs 74048/74095 and the `log stream` process 25842 were killed directly) before completion. **Only one live run's data is used above** — this entry does not claim two confirmed live runs, unlike the two-baseline table in the prior "structural fix" session. The single run obtained is nonetheless a genuine, complete, real live-model repro (not synthetic), with a clear and internally-consistent correlation pattern (steps 0–7 clean, steps 8–23 clustered), which is why this session treats it as sufficient evidence rather than inconclusive.

**Build/test results:** `swift build` clean, `swift test` **191/191 pass**, no regressions, no new tests (no behavioral code changed — this was a pure investigation-and-correlation session, same category as the immediately-prior session's negative-result stress testing).

**Status:** The catastrophic, permanent, second-process-triggered truncation mechanism remains fixed (do not re-open — no evidence from this session contradicts it; this run preserved ~101% of the session). The residual, milder, self-recovering PTS-gap-stall issue is now **correlated with real evidence for the first time**: it clusters around real, dynamic, network-loaded on-screen content (a live web page loading/animating/being scrolled) rather than any specific sidecar RPC, shows zero writer-side frame drops, and causes no net video-duration loss (frames resume; the stalled stretches are visually stale for a few seconds but not missing). None of this task's three candidate fixes (cache-latest-frame for `captureFrame`, queue-instead-of-drop for the writer, or a hybrid) are supported by this evidence, so none were applied — implementing one anyway would be exactly the kind of speculative, evidence-free fix this bug's history has repeatedly warned against. This residual issue remains open but is now well-characterized enough that a future session's most promising next step is OS-level tracing (`log stream`/Instruments) bracketing one of these clustered-stall windows specifically (e.g. around t=100s–172s of a repro matching this session's), looking at what `SCStream`/`replayd`/`WindowServer` are doing during a real multi-second gap — the same technique that found the *catastrophic* mechanism's root cause two sessions ago, not yet applied to this milder one.
