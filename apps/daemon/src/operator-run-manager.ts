/**
 * @deprecated `OperatorRunManager` was renamed to `OperatorRunEngine` and
 * moved to `@windower/engine` in Phase 20's `@windower/engine` extraction
 * (`phase-20-daemon-optional.md`). Re-exported here (under its new name) for
 * one minor version for backward compat; import `OperatorRunEngine` from
 * `@windower/engine` directly going forward.
 */
export { OperatorRunEngine, type OperatorRunEngineOptions } from "@windower/engine";
