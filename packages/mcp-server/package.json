{
  "name": "@windower/mcp-server",
  "version": "0.1.4",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "windower-mcp-server": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc --build && chmod +x dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --build --emitDeclarationOnly false --noEmit",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@windower/core": "workspace:*",
    "@windower/engine": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
