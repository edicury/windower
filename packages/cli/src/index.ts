/**
 * @windower/cli — the `windower` binary, a thin wrapper over @windower/core.
 *
 * Phase 0: placeholder export only, no functional capability yet.
 * See specs/001-windower-mvp/contracts/cli.md.
 */
export const CLI_PACKAGE_NAME = "@windower/cli";
