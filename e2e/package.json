{
  "name": "@windower/e2e",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "Real-daemon + real-sidecar end-to-end suite (Phase 13). Locally-gated: needs Screen Recording/Accessibility/Microphone TCC grants that GitHub Actions macOS runners cannot grant non-interactively. Deliberately NOT wired into the `test` turbo task — see README.md and CLAUDE.md's CI note.",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --emitDeclarationOnly false --noEmit",
    "clean": "tsc --build --clean",
    "test:e2e": "vitest run --config vitest.e2e.config.ts",
    "test:soak": "vitest run --config vitest.soak.config.ts"
  },
  "dependencies": {
    "@windower/core": "workspace:*",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
